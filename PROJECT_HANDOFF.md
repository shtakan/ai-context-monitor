# PROJECT_HANDOFF.md (для нового чата)

## Состояние проекта

**Версия:** 2.0.2 (релизный коммит v2.0.2 = `b00a532`, тег `v2.0.2`)  
**HEAD:** `b00a532` (v2.0.2); предыдущие — `072ae6a` (O-18), `07f45ce` (O-17), `a094920` (O-16), `c77fb90` (O-15)  
**Тесты:** 80 suites, 1367 passed, 6 skipped, 0 failed  
**Статус:** v2.0.2 выпущен — тег `v2.0.2` запушен, GitHub Release опубликован. O-15 (`c77fb90`), O-16 (`a094920`), O-17 (`07f45ce`), O-18 (`072ae6a`) закрыты и **приняты живыми прогонами**. Дистрибуция — GitHub Releases + секция «Ручная установка»; Edge Add-ons отложены бессрочно (комплаенс-блок RU-аккаунта, reference 715-123225)  

## Ключевые достижения

### Функционал
- **6 адаптеров** с единым контрактом (`BaseAdapter`)
- **Автоэкспорт** при достижении порога (работает на всех 6 платформах)
- **Ручной экспорт** в 4 форматах (md/json/pdf/txt)
- **Проактивные пороги** 70/85/95% с Chrome-уведомлениями
- **Импорт архивов** (Gemini Takeout, ChatGPT conversations.json, Perplexity, Claude)
- **BYOK** (Google AI Studio API key) для точного подсчёта токенов

### Архитектура
- **Strategy pattern** для адаптеров
- **Чистые функции** в `utils/` (export-emit-pipeline.js)
- **UMD-паттерн** (работает в браузере и Node.js тестах)
- **MV3-совместимость** (нет eval, нет удалённого кода, минимум permissions)
- **Traceability** (комментарии с версиями: v81, M-11, E-2, T1-fix#3)
- **Декомпозиция v2.0 (этап 1/3, ПРИНЯТ — `370a217`):**
  - `content.js` (1897 строк, было 3622) — 5 модулей:
    - `state.js` (416 строк) — глобальное состояние
    - `widget.js` (510 строк) — виджет
    - `base-handler.js` (218 строк) — обработка базы
    - `hybrid-tail.js` (162 строки) — гибридный хвост DOM
    - `export-manager.js` (945 строк) — экспорт
  - 153 элемента перенесены байтово, логика не изменена
  - Тесты обновлены только в строках резолва (23 файла)
- **Декомпозиция v2.0 (этап 2/3, ПРИНЯТ — `994ae26`, частичный):**
  - `gemini-intercept.js` 5064 → 4800 строк; вынесен `core/gemini-hidden-scroll.js`
    (356 строк, 13 функций + 10 констант, своя IIFE + UMD `window.AiCmGeminiHiddenScroll`)
  - Интеграция: `__bind` с геттерами/сеттерами живых переменных (не копии);
    29 ссылок `D.<имя>` в телах модуля
  - Кластеры network/parser/pagination/loader НЕотделимы (AST-замыкание 84–95%,
    218/259 общих имён) — перенос на этап 3/3 (ES modules)
  - Регистрация перехватчика — программно в `background.js`
    (`world:'MAIN'`, `runAt:'document_start'`), НЕ через manifest.json  
- **Декомпозиция v2.0 (этап 3/3, ПРИНЯТ — `be1a4a6`):** миграция id регистрации
  `ai-cm-gemini-intercept-v2` + `unregisterContentScripts` старого; ES modules для
  content scripts отложены до бандлера (решение — `ARCHITECTURE_STANDARDS.md`)
- **Релиз v2.0.0 (`3191cba`):** бамп версии + миграция 8 версионных сьютов +
  CHANGELOG/футер/listing + убран ключ-комментарий `//ESM`; **тег `v2.0.0` есть**
- **Релиз v2.0.1 (`be93f45`):** O-1 (бейдж ChatGPT без моргания после F5) +
  миграция 8 версионных сьютов + ручная установка без магазина (`1bf630f`);
  **тега/Release у v2.0.1 нет — промежуточная**
- **Релиз v2.0.2 (`b00a532`, тег `v2.0.2`):** надёжность DeepSeek-экспорта (O-15…O-18)
  + бамп версии + CHANGELOG + миграция версионных сьютов + футер/listing;
  **GitHub Release опубликован** —
  https://github.com/shtakan/ai-context-monitor/releases/tag/v2.0.2

**Итог по релизам v2.0:** все этапы декомпозиции 1/3, 2/3, 3/3 — **ПРИНЯТЫ**;
v2.0.0 (`3191cba`, тег `v2.0.0`) → v2.0.1 (`be93f45`, без тега/Release) →
v2.0.2 (`b00a532`, тег `v2.0.2`, Release опубликован).

## Критические проблемы (требуют рефакторинга)

### God-objects (senior-аудит 2026-09-13)

| Файл | Размер | Проблема |
|---|---|---|
| `core/gemini-intercept.js` | 330 KB | Перегружен: перехват + парсинг batchexecute + пагинация + loader + hidden scroll + Deep Research dedup |
| `core/content.js` | 229 KB | 30+ глобальных переменных, смешение ответственностей (state/widget/base/hybrid-tail/export) |

### План декомпозиции v2.0

**content.js →** (ЭТАП 1/3, ЗАВЕРШЁН И ПРИНЯТ, commit `370a217`)
- `state.js` (глобальное состояние: base/baseComplete/baseSeen)
- `widget.js` (виджет: createWidget/updateWidget/resetWidget)
- `base-handler.js` (обработка базы из сети)
- `hybrid-tail.js` (гибридный хвост DOM)
- `export-manager.js` (авто/ручной экспорт)

**gemini-intercept.js →** (ЭТАП 2/3, ЗАВЕРШЁН ЧАСТИЧНО И ПРИНЯТ, commit `994ae26`: только hidden-scroll; network/parser/pagination/loader перенесены на 3/3)
- `gemini-network.js` (перехват fetch/XHR)
- `gemini-batchexecute-parser.js` (парсинг protobuf + JSON)
- `gemini-pagination.js` (cursor, retries)
- `gemini-loader.js` (loader-detection)
- `gemini-hidden-scroll.js` (opacity=0, scrollTop=0)

**Переход на ES modules** (ЭТАП 3/3, ЗАВЕРШЁН ЧАСТИЧНО, commit `be1a4a6`)
- ✅ Миграция id регистрации `ai-cm-gemini-intercept-v2`
- ⏳ ES modules (`"type": "module"` в manifest, снижение глобального состояния) —
  отложены до бандлера (`ARCHITECTURE_STANDARDS.md`)

## Пост-релизная очередь low

| № | Проблема | Статус / Влияние |
|---|---|---|
| O-15 | DeepSeek live-экспорт: потеря ходов и усечение 2 символов на границах фрагментов | ✅ ЗАКРЫТ И ПРИНЯТ live (`c77fb90`): debounce автоэкспорта 800 мс + sync-flush manual |
| O-16 | Усечение ответа при автоэкспорте во время активного стрима | ✅ ЗАКРЫТ И ПРИНЯТ live (`a094920`): APPEND/SET фрагменты, терминал не замораживает ход, обогащение ходов |
| O-17 | SPA-созданный чат (без F5): база и turnsMap неполны → автоэкспорт с `[LOW CONFIDENCE]_` | ✅ ЗАКРЫТ И ПРИНЯТ live (`07f45ce`): ре-эмит базы при 0→1, авторитетный `baseEmpty`; см. «Приложение O-17» |
| O-18 | DeepSeek: рассинхронизация SSE-парсера фрагментов + неполнота истории в момент экспорта | ✅ ЗАКРЫТ И ПРИНЯТ live (`072ae6a`): белый список типов + resync из сырого кольца, сетевой дозапрос истории при экспорте (deepseek-intercept v12) |
| O-19 (НОВЫЙ) | Приёмочный прогон с включёнными соседними расширениями (DeepSeek++, опц. All API Hub) — измерение остаточных расхождений live vs network | **Открыт (Medium):** ожидание — расхождения объясняются только инъекциями соседей в серверной истории, механическая потеря отсутствует |
| O-20 (НОВЫЙ) | Санация известных маркеров сторонних расширений в тексте экспорта (`<!-- deepseek-pp-* -->`, тул-схема-преамбулы) | **Открыт (Medium):** фикстура с маркерами → вырезано; фикстура без маркеров → побайтово идентично |
| O-9-подпись | `snapshot-at-manual` печатает `msgs=0` на Perplexity | **Открыт (Medium):** косметика диагностики; тело экспорта полное |
| O-11 | Две маски имён автоэкспорта + коллизия на GSA | **Открыт (Medium):** потеря копии за ту же минуту; данные целы |
| O-1 | DOM-мисдетект ChatGPT ~1 c после F5 (мгновенный «морг» бейджа; итоговая цифра верная) | **Открыт (Medium):** возвращён в очередь; итоговая цифра верная |
| O-7 | Экспорт DeepSeek без reasoning-цепочек (процент честный; файл без невидимого текста) | **Открыт (Medium):** возвращён в очередь; продуктовое решение |
| O-14 | Попап держит старый снимок после RESET в пустой чат | **Открыт (Low):** косметика попапа |

**Платформенная заметка (не дефект):** GSA умеет урезать историю сервером (тред kk49: 59→6 сообщений). Расширение считает то, что сервер реально отдал.

**Edge Add-ons:** отложены бессрочно (комплаенс-блок RU-аккаунта, reference 715-123225). Основная дистрибуция — GitHub Releases + секция «Ручная установка» в README/docs; `tools/edge-listing-metadata.txt` сохранён на будущее.

## Технические детали

### Коммиты (последние 5)
```
b00a532 v2.0.2: надёжность DeepSeek-экспорта — O-15…O-18 (парность ходов, debounce стрима, полнота SPA-чата, ресинк SSE + сетевой дозапрос)
072ae6a fix(O-18): DeepSeek — ресинхронизация SSE-парсера фрагментов и сетевой дозапрос в момент экспорта
07f45ce fix(O-17): DeepSeek — полнота базы и turnsMap в SPA-созданном чате (экспорт без F5)
a094920 fix(O-16): DeepSeek — предотвращение усечения ответа при автоэкспорте во время активного стрима
c77fb90 fix(O-15): DeepSeek-экспорт — парность ходов и целостность фрагментов reasoning/answer
```

### Структура проекта
```
ai-context-monitor-clean/
├── manifest.json (MV3, 3 permissions, 9 host_permissions, version 2.0.2)
├── core/
│   ├── content.js (1897 строк) ✅ декомпозирован (этап 1/3)
│   ├── state.js (416 строк) ✅
│   ├── widget.js (510 строк) ✅
│   ├── base-handler.js (218 строк) ✅
│   ├── hybrid-tail.js (162 строки) ✅
│   ├── export-manager.js (945 строк) ✅
│   ├── background.js (38 KB)
│   ├── gemini-intercept.js (4800 строк) — ядро связано (84–95%), остаток на бандлер
│   ├── gemini-hidden-scroll.js (356 строк) ✅ вынесен (этап 2/3)
│   ├── claude-intercept.js (88 KB)
│   ├── deepseek-intercept.js (54 KB, v12: O-16 + O-17 + O-18)
│   ├── google-search-intercept.js (54 KB)
│   ├── perplexity-intercept.js (30 KB)
│   └── page-intercept.js (34 KB)
├── adapters/
│   ├── base-adapter.js (единый контракт)
│   ├── chatgpt-adapter.js
│   ├── gemini-adapter.js
│   ├── deepseek-adapter.js
│   ├── google-search-adapter.js
│   ├── claude-adapter.js
│   └── perplexity-adapter.js
├── utils/
│   ├── export-emit-pipeline.js (67 KB, чистые функции)
│   ├── gemini-intercept-logic.js (113 KB)
│   ├── gemini-batchexecute-parser.js (29 KB)
│   ├── google-search-folwr-parser.js (27 KB)
│   └── archive-import.js (36 KB)
├── options/options.js (57 KB)
├── tests/ (80 suites; helpers/content-source.js, helpers/gemini-intercept-source.js)
├── CHANGELOG.md
├── RELEASE_CHECKLIST.md
└── tools/edge-listing-metadata.txt (на будущее, для Edge Add-ons)
```

### Конвенции

**Кодер:** DSH Desktop (DeepSeek Harness), workspace `ai-context-monitor-clean`  
**Модель:** deepseek-flash  
**Промпт кодеру:** режим act; контекст-перенос ≤3 строк; задача пунктами; список «не трогать»; ответ = diff + отчёт ≤10 строк  
**Сессии:** новая сессия для каждой задачи (не продолжать старую)

**Усилие рассуждений:** обязательный параметр в каждом промпте для DSH.  
- **Max** — архитектурные задачи (рефакторинг, декомпозиция, миграции)  
- **Medium** — мелкие фиксы (O-1, O-7, O-9-подпись, O-11, O-14, O-15, O-16, O-17, O-18, O-19, O-20)  
- **Low** — косметика (O-14 попап, комментарии)  
- **Формат:** первая строка промпта, перед `Режим: act`

**Тарификация DSH:** peak/off-peak (пик Пекин 09:00–12:00 и 14:00–18:00 = Екб 06:00–09:00 и 11:00–15:00; тяжёлые прогоны — вне пика)

## Конвенции процесса

Правила, выработанные в цикле O-15…O-18 (нарушение любого из них уже приводило
к ложной приёмке или к лишним итерациям).

1. **Два провала фикса одного симптома → следующий промпт только измерение.**
   Если симптом воспроизводится после второй итерации правок, следующая сессия DSH
   заказывает измерение (дампы, side-by-side, сырые payload) без правок поведения.
   Фикс — только после того, как измерение покажет, где именно теряются байты/ходы.
2. **Каждое утверждение с основанием.** Все утверждения в отчётах и рекомендациях
   помечены основанием: «лог строки N», «скриншот», «гипотеза, не проверено».
   Немаркированных гипотез не выдаётся.
3. **Критерий приёмки привязан ко времени.** Критерий, который не может пройти
   в живом диалоге (например, «файлы совпадают» при сравнении снимков, сделанных
   с зазором в несколько минут стрима), — бракованный и не выдаётся. Правильная
   формулировка: «снимки в пределах одной секунды» или «одновременно».
4. **Харнесс ≠ приёмка live-багов.** Зелёные тесты и харнесс не являются приёмкой
   live-багов. Приёмка = живое побайтовое равенство на захваченном payload в реальном
   браузере. Харнесс — лишь регресс-гарантия для известных фикстур.

## Окружение кодера

- **DSH Desktop (anywhere-labs)** стабилен на чистом наборе плагинов.
- **Лаунчер:** `C:\DSH\DSH.vbs` + `DSH.ps1` — Edge app mode, изолированный профиль `DSH-Edge-Profile`.
- **Плагины:** `dshmarket`, `dsh-api-cost`, `dsh-reasoning-effort`.
- **Правило:** установка плагинов — только при полностью остановленном DSH.
- **temperature:** обсуждение опубликовано в `deepseek-ai/deepseek-harness`
  (add temperature to `LlmCallConfigAdapterDefaults`).
- **vision:** DSH не передаёт изображения модели — нужен `dsh-vision-bridge`
  или ждать upstream.
- Файлы экспорта живых прогонов всегда лежат на C:\Users\oleg\Desktop — DSH при поиске образцов в живых прогонах начинает с этой папки.

### Приёмочная матрица (актуальна для v2.0.2)

| Критерий | Статус |
|---|---|
| Ядро 6 адаптеров (виджет=бейдж=лог, модель из сети, полная база, сбросы/латчи) | ✅ |
| Автоэкспорт (ChatGPT, Gemini×2, DeepSeek, Claude, GSA, Perplexity) | ✅ |
| Четыре формата из одного чата (md/json/pdf/txt) | ✅ |
| Ru-локаль (options/privacy/docs, футер v1.19.0) | ✅ |
| Тесты 80/80, 1367 passed, 6 skipped, 0 failed | ✅ |
| Версия 2.0.2 во всех точках вывода | ✅ |
| Публикация на GitHub (тег `v2.0.0`, тег `v2.0.2`, Release, ассет) | ✅ |
| O-15/O-16/O-17/O-18 live-приёмка DeepSeek | ✅ |

## Осознанные границы этапа 1/3

Эти элементы остались в `content.js` и кандидаты на бандлер:

1. **`aiCmLastSeenConvId`** — переменная оставлена в `content.js` из-за ReferenceError при инициализаторе (jsdom-прогон реального порядка загрузки выявил баг)
2. **Слушатель `ai-cm-full-history`** — source-level пин вместе с комментарием H23 (handshake самого `content.js`)

## Осознанные границы этапа 2/3 и решения

1. Chrome не перечитывает `js[]` под тем же `id` регистрации → смена id на
   `ai-cm-gemini-intercept-v2` + `unregisterContentScripts` старого:
   **сделано** миграционным коммитом `be1a4a6` (этап 3/3).
2. Тулчейн выноса (build/verify/smoke) лежит в `tools/` (gitignored), воспроизводим.
3. Красный Lint на main (дефект чистого чекаута: gitignored-артефакты + CRLF)
   вылечен CI-хотфиксом (`322fd7e`); Lint на main теперь зелёный.

## CI-хотфикс (commit `322fd7e`)

Проблема: Lint на main красный — 4 failed suites на чистом чекауте:
- `release-metadata.test.js` и `version-hygiene.test.js` читают gitignored-артефакт `tools/edge-listing-metadata.txt`
- `gemini-overlay-tape.test.js` и `gemini-floor-confirmed.test.js` — байтовые пины с `'\n'` падают на CRLF-чекауте (Windows + `core.autocrlf=true`)

Решение:
- `.gitattributes`: `*.js/.json/.html/.css/.md → text eol=lf`, `*.png → binary`
- `release.yml`: `.gitattributes` исключён из zip (не уезжает пользователю)
- Тесты: skip-логика точечная (гейт `describeListing` + `test.skip`), ассерты не ослаблены

Результат: workspace 78/1350/0.

---

## Приложение «O-17»: SPA-созданный чат (DeepSeek, экспорт без F5)

### Данные воспроизведения (наблюдаемые симптомы)

Новый чат создан через SPA-навигацию (без перезагрузки страницы). При пересечении порога автоэкспорт:

- **`baseComplete=false` навсегда** — вердикт полноты базы так и не взводится;
- **`snapshot-at-manual msgs=0`** при ручном экспорте — карта ходов пуста;
- **`deferred-timeout(60s)`** — по истечении 60 с автоэкспорт всё равно уходит
  с префиксом **`[LOW CONFIDENCE]_`**.

### Гипотезы

1. **Гейт полноты живёт только в ветке page-load:** при SPA-создании чата эта ветка
   не исполняется, поэтому `baseComplete` не может стать `true` событийно.
2. **`turnsMap` не пополняется network-writes при неполной базе:** записи сети
   отбрасываются, пока база считается неполной, — карта ходов остаётся пустой,
   и manual-экспорт печатает `msgs=0`.

### Приёмка (выполнена — O-17 закрыт и принят живым прогоном)

1. **live-автоэкспорт == экспорт после F5** — по составу и **побайтово**;
2. **manual в любой момент == база** — ручной экспорт из SPA-чата совпадает с базой;
3. Автоэкспорт срабатывает за ~1–2 с после пересечения порога (без F5);
4. Префикс `[LOW CONFIDENCE]_` не появляется;
5. `snapshot-at-manual` печатает `msgs>0`.

### Состояние фикса

- Коммит `07f45ce`: `core/deepseek-intercept.js` → **v11** (+ полнота базы и turnsMap
  в SPA-созданном чате), новый сьют `tests/adapters/deepseek-o17-spa-chat.test.js`
  (**11 тестов**); `tests/adapters/deepseek-o16-stream-export.test.js` — правка 1 строки.
- Харнесс: **ALL PASS** (57 проверок, в т.ч. 9× O-17 SPA; на pre-fix коде 7 FAIL).
- Статус: **ЗАКРЫТ И ПРИНЯТ ЖИВЫМ ПРОГОНОМ** на реальном SPA-чате (см. «Конвенции процесса», п. 4).

### Промпт DSH на O-17 (усилие: Medium; Max — если потребуется переработка гейта полноты)

```text
Усилие рассуждений: Medium
Режим: act
Задача: O-17 — DeepSeek: экспорт в SPA-созданном чате без F5

## Контекст
Workspace ai-context-monitor-clean. Файл core/deepseek-intercept.js (v10, O-16 закрыт).
Новый чат создан SPA-навигацией, страница не перезагружалась.

## Симптомы (воспроизведение)
1. baseComplete=false навсегда — вердикт полноты базы не взводится.
2. snapshot-at-manual печатает msgs=0 — карта ходов пуста.
3. deferred-timeout(60s) — автоэкспорт в итоге уходит с префиксом [LOW CONFIDENCE]_.

## Гипотезы (проверить и подтвердить/опровергнуть)
1. Гейт полноты исполняется только в ветке page-load; при SPA-создании чата
   baseComplete не может стать true событийно.
2. turnsMap не пополняется network-writes, пока база считается неполной.

## Требуется
- Починить полноту базы и пополнение turnsMap для SPA-созданного чата
  (ре-эмит базы при переходе полноты 0→1, авторитетный признак baseEmpty).
- Разобраться с deferred-timeout: [LOW CONFIDENCE]_ при полной базе не появляется.

## Приёмка
1. live-автоэкспорт == экспорт после F5 (состав и байты).
2. manual в любой момент == база.
3. Автоэкспорт за ~1–2 с после порога, без F5.
4. Префикс [LOW CONFIDENCE]_ отсутствует; snapshot-at-manual msgs>0.

## Не трогать
Другие адаптеры, export-emit-pipeline.js, схему имён файлов, публичные контракты.

## Ответ
diff + отчёт ≤10 строк; тесты — новый сьют tests/adapters/deepseek-o17-spa-chat.test.js.
```

---

## Следующие шаги

1. ✅ Обновление GitHub (Release v1.19.3)
2. ✅ Этап 1/3: декомпозиция content.js → 5 модулей (`370a217`) — принят
3. ✅ Этап 2/3: частичная декомпозиция gemini-intercept.js (`994ae26`) + живой прогон — принят
4. ✅ CI-хотфикс (`322fd7e`) — Lint на main зелёный
5. ✅ Этап 3/3: миграция id регистрации `ai-cm-gemini-intercept-v2` (`be1a4a6`) — принят
6. ✅ Релизный коммит v2.0.0 (`3191cba`) + тег `v2.0.0`
7. ✅ v2.0.1 (`be93f45`): O-1 + ручная установка без магазина — без тега/Release (промежуточная)
8. ✅ O-7 (`2866474`), O-15 (`c77fb90`), O-16 (`a094920`)
9. ✅ **O-17** (`07f45ce`) — принят живым прогоном на реальном SPA-чате
10. ✅ **O-18** (`072ae6a`) — принят живым прогоном (ресинк SSE + сетевой дозапрос)
11. ✅ **Релизный коммит 2.0.2** (`b00a532`) — бамп версии + CHANGELOG + миграция версионных сьютов + футер/listing
12. ✅ **Тег `v2.0.2` + GitHub Release** (основная дистрибуция) — опубликован
13. ⏳ **Очередь:** O-19 → O-20 → O-9-подпись → O-11 → O-1 → O-7 → O-14
14. ⏳ **Чистка хвостовых дублей HANDOFF** («Этап 1/3… в процессе» и т.п.) — выполнено в предыдущей ревизии

**История этапов 1/3–3/3** — см. секции «Декомпозиция v2.0» и «Следующие шаги» выше.

---

## Контроль длины чата

Резюме актуально на 15 сентября 2026 (после релиза v2.0.2): этапы 1–3/3 приняты, CI-хотфикс, релизы v2.0.0 (`3191cba`, тег), v2.0.1 (`be93f45`, без тега/Release) и v2.0.2 (`b00a532`, тег `v2.0.2`, GitHub Release опубликован); O-7/O-15/O-16/O-17/O-18 закрыты и приняты живыми прогонами; тесты 80 suites / 1367 passed / 6 skipped / 0 failed; ES modules для content scripts отложены до бандлера (`ARCHITECTURE_STANDARDS.md`); Edge Add-ons отложены бессрочно (комплаенс-блок RU-аккаунта, reference 715-123225).  
Следующая синхронизация — после закрытия очередного пункта очереди low (O-9-подпись → O-11 → O-14 → O-19 → O-20) или следующего релиза.
