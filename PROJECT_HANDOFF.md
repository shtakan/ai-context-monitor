# PROJECT_HANDOFF.md (для нового чата)

ДАЙДЖЕСТ СОСТОЯНИЯ ai-context-monitor (2026-09-23, после закрытия O-23/O-24/O-29/O-32/O-34/O-43/O-45/O-46; голова — O-21; O-32 ЗАКРЫТ живой приёмкой, O-46 ЗАКРЫТ коммитом d3d0ea0)
1. Источники истины: manifest.json (версия), вывод npm test, git log, PROJECT_HANDOFF.md (§0 + верхний блок), RELEASE_CHECKLIST.md (локальный, H26).
2. Опубликован v2.0.11: тег v2.0.11 на docs-коммите 0892033; GitHub Release, Release ZIP #14, asset 2.7 MB (sha256 b17cbe63…); Lint #37 зелёный. HEAD=e1b894f (docs: update HEAD to final residual-fix commit); предыдущий HEAD=d1af0cc (docs: fix residual O-22..O-24 status in open queue line 213); ранее HEAD=50c02ec (chore(O-46): google-search-folwr-parser в content_scripts[].js); docs-синк handoff — коммит поверх него (реальный HEAD после синка — этот docs-коммит). Ветка main (ahead of origin/main на 8 коммитов, база origin/main=46ae869: 7d66180 (fix O-32) → d3d0ea0 (chore O-46) → ea99ab1 (docs-синк O-32/O-46) → 6584004 (docs: fix O-46 open status) → 50c02ec (docs: fix O-21..O-24 drift) → 5aa08f6 (docs: update HEAD) → d1af0cc (docs: fix residual O-22..O-24) → e1b894f (docs: update HEAD to final residual-fix commit)); рабочая копия чиста. Сьют: 120 suites / 2167 passed / 6 skipped / 0 failed. Коммиты: 924e878 (O-23) → 02d60c9 (O-24) → 239ac61 (O-34) → 0fb6d92 (.gitignore) → 1faf891 → ea9bee3 → ff56461 → c322c77 → 71344b4 → d875daf → fix-O-43 → fix-O-29 → docs-O-45 → 9fa9cec → dd250c2 (docs-синк O-45) → 46ae869 (docs O-32) → 7d66180 (fix O-32) → d3d0ea0 (chore O-46) → ea99ab1 (docs-синк O-32/O-46) → 6584004 (docs O-46 open status) → 50c02ec (docs O-21..O-24 drift) → 5aa08f6 (docs HEAD) → d1af0cc (docs residual O-22..O-24) → e1b894f (docs HEAD); целостность HEAD верифицирована.
3. Закрыто в цикле 09-19→09-21: O-35, O-36, O-37, O-38, O-39(B), O-40, O-42, Claude md. R1–R6 выполнены 09-21.
4. Обновление 2026-09-22 (O-23 ЗАКРЫТ как expected behaviour; O-24 ЗАКРЫТ живой приёмкой; O-34 ЗАКРЫТ живой приёмкой):
   - O-23 (F5: tokens~ vs serverTokens) ЗАКРЫТ как expected behaviour (senior-рекомендация): расхождение +128.7% (convId 49846f0e, textLen=424184, cyrillicPct=73.8, tokensTilde=223358, serverTokens=97652) объяснено несовпадением базиса счёта (природа c), а не ошибкой коэффициента языка (a) или отсутствием reasoning (b). Делитель кириллицы 1.8 подтверждён 3 точками и BYOK-эталоном (~1.97); глобальное изменение сломает P1/P3. serverTokens = accumulated_token_usage (накопитель), не эквивалентен полному textLen. Бейдж DeepSeek авторитетен по serverTokens; tokens~ = фолбэк при serverTokens=0 с границей ±10%. Пост-фактор адресного сжатия отклонён как хрупкая калибровка по аномалии. Диффов в аналитическом раунде 09-22 нет; код-правки O-23 — в 924e878 (utils/tokenizer.js, core/content.js; 4 файла, +322/−17). Сьют калибровки 19/19 зелёный (14 + 5).
   - O-24 (F6: зацикленный svc-emit после already-fired) ЗАКРЫТ живой приёмкой 2026-09-22: сигнатурный гард в MutationObserver колбэке (aiCmDomTextSig = baseCount|DOM-length + латч aiCmSiteConvFired + исключение #ai-context-widget в aiCmIsWidgetNode). Тесты: 115 suites / 2052 passed / 6 skipped / 0 failed (+13 пинов tests/o24-svc-emit-loop-guard.test.js). Живая приёмка: после fired нет повторов svc-emit-trace с каденсом 1-2 с (только o24-dom-emit-guard verdict=skip-sig-unchanged); при новом ходе сигнатура меняется и легитимный эмит проходит.
   - O-34 (N1: дорисовка остаточных значений на URL без /c/<id>) ЗАКРЫТ живой приёмкой 2026-09-22: фикс shouldResetConversationOnNav в utils/chatgpt-conversation-parser.js (смена контекста включая потерю id) + aiCmResetStaleStateOnChatlessChatGptUrl в core/content.js (гейт chatgpt + пустой getCurrentConvId → resetConversationState + BADGE_RESET до createWidget). Тесты: 116 suites / 2072 passed / 6 skipped / 0 failed (+20 пинов tests/o34-reset-on-empty-convid.test.js). Живая приёмка: SPA-переход на New Chat → бейдж 0%/56 токенов (системный промпт нового чата); F5 на корне chatgpt.com → бейдж 0.1%/70 токенов (маркер O-34 в консоли: «URL без /c/<id> — состояние виджета и бейдж сброшены»); DeepSeek/Gemini байтово прежние.
   - O-29 (Low, лог-спам автоэкспорта GSA) ЗАКРЫТ живой приёмкой 2026-09-22: антиспам-гард `aiCmAutoExportLastLogState` (family→сигнатура) + хелперы `aiCmAutoExportLogOnChange`/`aiCmAutoExportLogFiredBit`; лог только на смену состояния; сьют 117/2096/6/0 (+24 пина tests/o29-autoexport-log-spam.test.js). Живая приёмка: строка `latch kept reason=spa-entry` появилась ровно 3 раза за сессию (строго на смену threadId), `skip reason=already-fired` — ровно 1 раз после файра; непрерывного спама нет.
   - O-43 ЗАКРЫТ живой приёмкой 2026-09-23: файл по полной сетевой базе (netLatch=true, msgs=54, textLen=56884), обрыва истории нет; сьют 118/2117/6/0. Корень (Medium-High; измерен 09-22): race condition автоэкспорта GSA — файл по memory-snapshot (22 msgs, textLen=31541) до дозревания network-базы (32 msgs, textLen=58989); дельта 262 мс между стартом скачивания (21:15:04.533) и принятием полной базы (21:15:04.795). ЖИВОЕ ИЗМЕРЕНИЕ (09-22, гейт aiCmDebug, длинный чат 20 msgs/textLen=29527 и короткий 1 msg): memory→network дельта 264–504 мс, окно НЕ ограничено сверху (пагинация folwr +400 мс/страница); корень уточнён — не «histSource=memory до mergeTurns», а РЕГРЕССИЯ вердикта полноты: эмит с historyComplete=true перекрывается последующими эмитами ТОГО ЖЕ треда с historyComplete=0 (handshake-переэмит + повторный open-путь после probe), поэтому delay-N-ms и гейт «histSource=network» не работают. РЕШЕНО кодом (O-43, зелёный): монотонный латч сетевой полноты `aiCmGsaNetworkCompleteLatch` ('site|threadId', core/state.js) — взвод в приёме снимка с historyComplete=true (core/content.js), НЕ снимается регрессией, сброс только на смене треда (core/widget.js:resetConversationState), чтение в гейте (utils/export-emit-pipeline.js:shouldSkipAutoExport, поле gsaNetworkCompleteLatch; у прочих сервисов undefined → вердикты прежние); пины tests/o43-gsa-autoexport-race.test.js (D1/D2/D3/D4/R1/R2).
   - O-45 ЗАКРЫТ живой приёмкой 2026-09-23: экспорт содержит полный текст код-блоков с маркером языка (python/javascript; файлы 11-05/11-08 от 2026-09-23 10:03–11:08); сьют 119/2143/6/0; корень: `utils/google-search-folwr-parser.js:gatherAnswerContentNodes` не собирал код-контейнеры (`pre/code/[class*=code-block]`); фикс — селекторы + хелпер `renderCodeBlock` (фенс + lang + textContent) переиспользуется сетевым/DOM-путём и адаптером; 26 пинов `tests/o45-gsa-code-blocks-export.test.js`. Наблюдение (отклонение от отчёта кодера): fence-бактикы (```) в txt-экспорте не наблюдаются (код = строка-метка + тело); сверка с пинами D1/D6 перед коммитом фикса.
   - O-46 ЗАКРЫТ 2026-09-23 (коммит d3d0ea0 поверх 7d66180): в `content_scripts[0].js` манифеста добавлена строка `utils/google-search-folwr-parser.js` (сразу после `utils/gemini-batchexecute-parser.js`; ровно +1 строка, версия 2.0.11 не менялась) — единый DOM/сетевой путь без фолбэка адаптера, `window.GoogleFolwrUtils` виден и в ISOLATED-мире адаптера; сьют 120/2167/6/0; строка таблицы O-46 и п. 21 очереди приведены к статусу ЗАКРЫТ.
   - Наблюдение (вне scope O-43, живой лог): петля чужого треда GSA — submit + pagination skip-no-gain каждые ~400 мс телом 1.36–1.97 МБ, virtualScrollBackfill('pagination-empty'); сетевой/ресурсный шум, отдельная задача.
   - O-32 ЗАКРЫТ живой приёмкой 2026-09-23: инвариант канонической идентичности ходов GSA (canonicalTurnKeyOf + mergeTurnsMonotone + доказанная полнота historyComplete===true); бейдж стабилен 4.6%→4.5%→4.6%→7.8%→7.8%→7.9% при F5/SPA/+2 обменах; хеш отпечатка 0f9f7baa (6 ходов) и 32e4a45b (8 ходов) идентичен на всех точках; сьют 120/2167/6/0 (+23 пина tests/o32-gsa-canonical-thread-invariant.test.js); код O-32 зафиксирован коммитом 7d66180 (15 файлов, +1096/−47) — ранее код лежал незакоммиченным при docs-коммите 46ae869.
   - Очередь: голова — O-21 (O-46 закрыт 2026-09-23).
5. Открыто: (а) решение владельца а/б по наблюдению O-39(B): строка reasoning виджета на SPA-открытии исчезла (usage истории без reasoning_tokens; netServerTokens=0 при history-записи — не исследовано); (б) селекторы adapters/qwen-adapter.js:46-67 и contextLimit 128 000 (utils/model-config.js:203, комментарий-HYPOTHESIS там же) — гипотезы, живьём не опровергнуты, измерением не закрыты; (в) принятые отклонения: O-40 спан формы A не снимается, O-42 пустые пары <original_task>/<tool_results> снимаются только внутри распознанного конверта, 4 точки защиты Claude md оставлены.
6. Конвенции в силе: WIP=1 (одна задача = один чат); промпт кодеру = один блок из четырёх обратных кавычек (тройные внутри запрещены), первая строка «Усилие рассуждений: Max|Medium|Low», затем «Режим: act», перенос «Принцип качества: §0 handoff (стандарты + senior-уровень)
Формат ответа: факты с источником + ОДНА senior-рекомендация; ≤20 строк; без меню вариантов, без пересказа команд и процедур, уже записанных в handoff, без видимой секции самопроверки; ограничения и уверенность — 1–2 строки в конце.»; правило R-D; H26 (RELEASE_CHECKLIST.md и docs/QUALITY_AUDIT.md gitignored, force-add запрещён); handoff править только явным UTF8; CHANGELOG трогается только в релизном коммите; приёмка включает инспекцию файла экспорта (findstr / счётчики Notepad++); F5 = новый instance (латч O-38 пуст, первый файр штатен).
7. Уроки процесса этого чата: (а) программа приёмки без пункта «инспекция файла» — ошибка (пропуск D2); (б) владельцу НЕ давать задания «сравнить побайтово с прежним» — проверки должны быть самопроверяемыми (счетчики совпадений, маркеры), владелец не хранит эталоны в голове; (в) повторный файр автоэкспорта имел корнем латч в состоянии виджета (O-38); (г) инъекции соседей санятся универсально по формам (O-40/O-42), а не точечно по маркерам.
8. Вход в следующий чат — две строки: «Источник истины: PROJECT_HANDOFF.md (§0 + верхний блок). Задача: O-32 (Medium, механизм индикатора/процента; спецификация — строка таблицы очереди).
9. Имя подхода: handoff-driven small-batch workflow (разделяй-и-властвуй + WIP=1 + атомарные коммиты + SSOT-handoff); возврат в «проект целиком» только через верхний блок handoff, чаты одноразовы.
10. Артефакты чата (логи 09-20 11:26–11:29 — воспроизведение O-38; экспорты 11-34/11-40 — артефакты O-39/O-40; скриншоты приёмки) — файлы владельца на Desktop; удаляются регулярно, отсутствие — дефолтное допущение для новых сессий.
11. GitHub Release v2.0.11 и Lint #37 не проверены локально (read-only аудит); сверить при следующем онлайн-шаге через gh release view v2.0.11.

## Вход в новую сессию (читай, ассистент, ДО любых действий)
- Единственный источник состояния проекта = этот файл в папке `C:\Users\oleg\Desktop\Prompts\ai-context-monitor-clean`. Папка `ai-context-monitor-v2.0.10` на диске — архивная копия (состояние до O-38, до v2.0.11): не открывай её, не бери код/состояние оттуда (инцидент 2026-09-21: новая сессия прочитала копию и работала на устаревшем состоянии).
- MCP включён: чтение файлов — через filesystem-инструменты, пачкой (один list + один таргет-файл); без слова владельца дополнительных чтений не делай.
- Спецификация следующей задачи (O-29) — в таблице открытых дефектов ниже (строка «O-29 | Лог-спам автоэкспорта GSA...»); читай перед сессией O-29.
- Протокол Правды: вклей его текст в первое сообщение сессии ОДИН раз, не прикрепляй файл к каждому сообщению.
- Формат ответа координатору: факты с источником + ОДНА senior-рекомендация; ≤20 строк.

## §0. Принцип качества (неоспоримый)

1. Расширение соответствует стандартам: Chrome MV3 best practices; программные политики Chrome Web Store как эталон качества (дистрибуция — GitHub Releases); надёжность перехватчиков (исключение внутри враппера не должно ломать хост-страницу); privacy-гигиена (нет innerHTML с контентом чата, ключи только в chrome.storage, тихая консоль без гейта aiCmDebug); тестируемость (каждый фикс — R-D пины на каждый путь записи базы и точку выхода).
2. Реализация, рецензия и приёмка — уровень senior и выше: нет фикса без корня; нет коммита без зелёной Chrome-проверки; нет релиза без зелёного сьюта и RELEASE_CHECKLIST; каждое отклонение от стандартов фиксируется в docs/QUALITY_AUDIT.md с ID и приоритетом P0/P1/P2 и либо чинится, либо осознанно принимается владельцем.
3. Действие: на старте сессии — из настоящего handoff; на каждом шаге — строкой контекст-переноса промпта кодеру; на релизе — гейтом RELEASE_CHECKLIST.md. Обновляющие блоки handoff могут перекрывать строки очереди и релизов, но не §0.

## Обновление 2026-09-22 (O-23 ЗАКРЫТ как expected behaviour; O-24 ЗАКРЫТ живой приёмкой; O-34 ЗАКРЫТ живой приёмкой)
Перекрывает конфликтующие места блоков ниже (включая строки очереди предыдущих обновлений):
O-23 ЗАКРЫТ (2026-09-22, read-only анализ P2): расхождение tokens~ (+128.7%) против serverTokens в convId 49846f0e объяснено несовпадением базиса счёта (природа c), а не ошибкой коэффициента языка (a) или отсутствием reasoning (b). Делитель кириллицы 1.8 подтверждён 3 точками и BYOK-эталоном (~1.97); глобальное изменение сломает P1/P3. serverTokens = accumulated_token_usage (накопитель), не эквивалентен полному textLen. Бейдж DeepSeek авторитетен по serverTokens; tokens~ = фолбэк при serverTokens=0 с границей ±10%. Пост-фактор адресного сжатия отклонён как хрупкая калибровка по аномалии. Статус: осознанно принято (senior-рекомендация). Диффов в аналитическом раунде 09-22 нет; код-правки O-23 — в 924e878 (utils/tokenizer.js, core/content.js; 4 файла, +322/−17). Сьют калибровки 19/19 зелёный (14 + 5).
O-24 ЗАКРЫТ (2026-09-22, живая приёмка): сигнатурный гард в MutationObserver колбэке (aiCmDomTextSig = baseCount|DOM-length + латч aiCmSiteConvFired + исключение #ai-context-widget в aiCmIsWidgetNode). Тесты: 115 suites / 2052 passed / 6 skipped / 0 failed (+13 пинов tests/o24-svc-emit-loop-guard.test.js). Живая приёмка: после fired нет повторов svc-emit-trace с каденсом 1-2 с (только o24-dom-emit-guard verdict=skip-sig-unchanged); при новом ходе сигнатура меняется и легитимный эмит проходит.
Очередь: голова — O-29 (Low); хвост: O-32 → O-21.

Обновление 2026-09-21 (релизная пачка 2.0.11 собрана; тег v2.0.11 на этом docs-коммите)
Перекрывает конфликтующие места блоков ниже (включая строки релиза предыдущих обновлений):
- HEAD: этот docs-коммит (поверх R2 пакетного коммита `fb68c71`). Рабочая копия чиста после коммита.
- R1 совмещённая приёмка завершена (2026-09-21):
  - O-39(B): Qwen history reasoning (SPA-переход/F5) — живая приёмка 2026-09-20 18:53–18:56: `[REASONING]`/`[ANSWER]` на месте.
  - O-40: санация инъекций Better DeepSeek (6 форм: F1–F6, F3 расширена на спан) — живая приёмка: 0 совпадений `<BetterDeepSeek>`, файл 268 КБ → 23 КБ.
  - O-42: санация конверта tool-continuation (4 дескриптора: local_file_read, prose, original_task, tool_results) — живая приёмка: `tool_results=0`, `<local_file_read>=0`.
  - Qwen reasoning toggle: OFF→0, ON→45 совпадений `[REASONING]`.
  - Claude md: 5 точек нормализации ролей (корень: `core/content.js:653`, защита: `base-handler.js:54`, `export-manager.js:178/215`, `export-emit-pipeline.js:567`, `export-text-builders.js:300`) — живая приёмка: чередование `## Пользователь` / `## Ассистент`.
  - Регресс шести платформ: DeepSeek/Perplexity/ChatGPT/Gemini/GSA — все 4 проверки зелёные.
- Отклонения (приняты владельцем через делегирование senior-рекомендации 2026-09-21):
  1. O-40: спан формы A (`local_file_read`) внутри сообщения не снимается (0 измеренных спанов в артефакте; литеральный паттерн ломает 11 замороженных тестов).
  2. O-42: пустая пара `<original_task>` и пара `<tool_results>` снимаются только внутри распознанного конверта (якорь prose-continuation); иначе F5/F6 после O-40 выпадали бы из экспорта.
  3. O-42: подпись тумблера обновлена также в `_locales/ru|en` (i18n-инвариант ru ↔ fallback).
  4. Claude md: 4 точки нормализации (base-handler:54, export-manager:178/215, export-emit-pipeline:567) не были корнем (корень = content.js:653), но оставлены как защита от прямой подачи `'human'` в билдер.
- Релизный коммит v2.0.11: db5e6e4 (11 файлов: manifest/package 2.0.11, CHANGELOG [2.0.11] Fixed с O-39(B)/O-40/O-42/Qwen reasoning toggle/Claude md + Changed подпись тумблера, футер docs/index.html, 7 версионных пинов включая qwen-provider-wiring + date-пин version-hygiene + edge-listing-metadata.txt на диске); package-lock.json и tools/edge-listing-metadata.txt обновлены на диске (untracked); сьют 113/2025/6/0 (число тестов не изменилось при релизном бампе).
- R2 пакетный коммит: `fb68c71` (45 файлов, 113 suites / 2025 passed / 6 skipped / 0 failed).
- Релизная пачка v2.0.11 собрана: R2 пакетный коммит fb68c71 → R3 docs b148002 → R4 релизный коммит db5e6e4 (manifest/package 2.0.11, CHANGELOG [2.0.11], 11 файлов, 47+/28-, сьют 113/2025/6/0) → финальный docs-коммит 0892033 (тег v2.0.11 на нём) → GitHub Release v2.0.11 (выполнено 2026-09-21).
- Публикация v2.0.11 (2026-09-21): тег v2.0.11 на 0892033 (push выполнен); GitHub Release v2.0.11 — Release ZIP #14, asset ai-context-monitor-v2.0.11.zip 2.7 MB (sha256 b17cbe632c77f4657d45a7b…); Lint #37 на 0892033 зелёный.
- Очередь: голова — O-23; далее O-24; O-34 (Low); хвост: O-29 → O-32 → O-21 (малоактуальна).

## СОСТОЯНИЕ 2026-09-20 (локальный блок; коммит — в docs-коммите R3):
- HEAD abd782a (origin/main). Рабочая копия НЕ закоммичена: O-35 реализован (A+C-lite: utils/stream-frames.js, core/qwen-intercept.js, adapters/qwen-adapter.js, manifest 9→10 хостов, core/background.js 7-я MAIN, core/content.js ветка qwen, utils/model-config.js qwen3.8-max 128K HYPOTHESIS, utils/export-text-builders.js [REASONING]/[ANSWER]) + O-36 (D1 доверие ручного снимка aiCmQwenExportBaseTrusted, D2 роль-маркеры txt/md + dropContainerMessages на экспорте, D3 shouldSkipBaseCompleteTrigger + гейт в export-manager) + O-37 (A domBaseTrusted в shouldSkipAutoExport + aiCmAutoExportTrustedBase, O-36.2 контейнер-чистка в extractMessages адаптера, C проводка reasoning aiCmPrepareReasoningForExport/applyReasoningExportFields). Сьют: 106 suites / 1859 passed / 6 skipped / 0 failed (после фикса O-38; база до фикса 105/1836/6).
- Живая приёмка Qwen 2026-09-20: холод+SPA инициализация зелёная; F5 зелёный; контейнер-чистка зелёная (extractMessages=73, отброшено 2; бейдж pct=21.4 tokens=27416 на 7fec9bd3); reasoning в попапе зелёный (reasoning=279, чат 2b7b72af).
- O-38 ЗАКРЫТ (фикс + живая приёмка 2026-09-20 12:56–13:00): латч порога автоэкспорта жил в состоянии виджета и сбрасывался resetConversationState → повторный файр при SPA-возврате (лог 11:29:25 reset → 11:29:30 fired convId=7fec9bd3). Фикс: aiCmAutoExportFiredOnce (Map site|convId) в core/state.js, widget о нём не знает; консультация в maybeAutoExport (pipeline+fallback), установка в doAutoExportDownload (кроме pre-trim); 10 тест-блоков (23 jest-кейса) tests/o38-autoexport-once-latch.test.js. Живая приёмка: SPA на A → файл 12-56; SPA на B → файл 12-58; SPA-возврат на A → файла нет (skip already-fired); F5 на A → файл 13-00 (новый instance, латч пуст — ожидание). Отклонение: кодер O-38 не гонял npm test (среда без shell/node), сьют гонял владелец (106/1859/6/0).
- ОТКРЫТЫЕ ДЕФЕКТЫ (корни измерены, фиксы не начаты):
  O-39 (reasoning-экспорт): фикс C из O-37 реализован и запинен, но живой txt-экспорт 11-34 (чат 2b7b72af, попап reasoning=279) без [REASONING]; корень в новом состоянии не измерен; гипотезы: (a) текст thinking summary не попал в поле захвата hiddenReasoning в этом прогоне, (b) путь ручного снимка теряет поле reasoning до билдера; измерение: логи qwen-reasoning/qwen-usage прогона 11:26 + читка пути снимок→билдер.
  O-39 сценарий 2 (2026-09-20 14:25): экспорт SPA-открытого чата 6ba32f54 (файл …-14-25) — 0 совпадений [REASONING] (поиск Notepad), на странице «Завершено размышление»; виджет reasoning=520 (то же значение, что у чата bade310b сценария 1) с маркером «· stale», бейдж 8.3%/10 643. Сценарий 1 (live-стрим bade310b, файл …-14-23) ПОСЛЕ фикса пройден: [REASONING]+[ANSWER] на месте. Вывод: фикс покрывает путь live-стрима; на пути истории-чата reasoning отсутствует уже в снимке (гипотеза). Следующий шаг — измерение без правок.
  O-39 сценарий 2 дополнен (2026-09-20 15-50): контрольная пара на одном чате bade310b — live-экспорт …-14-23 с [REASONING]+[ANSWER] и корректными ролями; SPA-реэкспорт …-15-50 без [REASONING], с новыми симптомами: два ASSISTANT со склейкой вопрос+ответ, дубликат склеенного блока, схлопнутые переводы строк. Вывод: путь истории ломает не только reasoning, но и состав сообщений; корень — писатель пути истории (гипотеза). Измерение read-only заказано; фикс не начат.
  O-39 сценарий 2 диагноз (2026-09-20 16:53): контрольная пара bade310b (live-экспорт …-14-23 vs SPA-реэкспорт …-15-50). Механизм 14-23: qwen-intercept composeTurnText (строки 363-365) вшивает [REASONING]/[ANSWER] в text на SSE → sections inside text → O-37 section parsing разбирает секции на сетевом пути. Механизм 15-50: SPA reopen → no SSE → adapter path (baseSeen=false) → qwen-adapter extractMessages → (a) reasoning отсутствует (DOM panel вне message-узла, _reasoningText ищет внутри), (b) контейнеры (chat-messages-container, chat-messages) матчат _messageSelectors → дубликат, (c) textContent concat без newlines → склейка вопрос+ответ + схлопнутые переводы, (d) container drop rule (candidate ≥ 2× longest contained) не срабатывает (192 < 276) → контейнеры выживают. Корни измерены; фикс не начат. Решение: разделить на O-39 (reasoning persist) + O-41 (adapter container/glue); scope и порядок — решение владельца.
  O-39 измерение Network (2026-09-20 ~17:00): endpoint истории Qwen измерен — GET /api/v2/chats/{id}?direction=up&limit=10, application/json; reasoning в payload есть: content_list[] элемент phase="thinking_summary", status="finished", extras.summary_thought.content = массив абзацев summary (совпадает с [REASONING] live-экспорта …-14-23), extras.summary_title.contents = заголовки шагов, role="assistant", response_id, usage{input_tokens,output_tokens}. Каждый контрольный фрагмент встречается в теле 2 раза (счётчики 1 of 2 / 2 of 2) — место второго вхождения не установлено; парсер фикс-сессии берёт первое thinking_summary-вхождение, дубль измеряется read-only. Вердикт матрицы: строка 1 — вариант B осуществим. Решение владельца: фикс-сессия O-39 = B; очередь по умолчанию O-39(B) → O-40 → O-41.
  O-39 (вариант B) ЗАКРЫТ кодом (2026-09-20 ~17:30): core/qwen-intercept.js — перехват JSON-истории GET /api/v2/chats/{id} через resp.clone().json(); парсер parseQwenHistory извлекает reasoning из content_list[].phase='thinking_summary' → extras.summary_thought.content[] (абзацы через \n\n); ходы в recordTurn → emitSnapshot → detail.messages[].reasoning + секции [REASONING]/[ANSWER] в тексте через composeTurnText; дубль reasoning в payload — первое thinking_summary-вхождение с текстом побеждает, измерение read-only qwen-history-duplicate под гейтом aiCmDebug. Тесты: 108/1905/6/0 (+25 пинов tests/qwen-o39-history-reasoning.test.js). Живая приёмка O-39 scenario 2 (SPA-переход/F5) не выполнена.
  O-39(B) живой прогон 18:09–18:25: файлы …-18-09 и qwen-14732181-…-18-14 без [REASONING]; в консоли под гейтом ни одной строки qwen-history. Корень измерен: GET истории чата идёт через XMLHttpRequest (Network шаг 1: Type=xhr, Initiator jquery.min.js:1), враппер O-39(B) оборачивает только window.fetch → isQwenHistory вживую не вызывается; тесты мокнули fetch, не XHR. Доза-фикс: XHR-хук эндпоинта истории (fetch-хук сохранить), пины на XHR-путь. Побочные наблюдения: чат 14732181 висел на «Загрузка контекста…» (причина не установлена; параллельно fourier.taobao.com ERR_CONNECTION_TIMED_OUT ×2, наш код ответ не меняет и XHR не трогает); индикатор 8% → 50%+ после скролла вверх = DOM virtual scroll (адаптер считает только отрендеренные сообщения: msgs 73 → 120, pct 8 → 63.5), будет закрыт серверной базой после XHR-хука.
  O-39(B) доза-фикс XHR (2026-09-20 ~18:30): core/qwen-intercept.js +138 строк — XHR-хук через XMLHttpRequest.prototype.open/send + addEventListener('loadend'), consumeHistoryXhr с ветками responseType (''/text → responseText+JSON.parse, 'json' → xhr.response, прочие → skip), try/catch вокруг тела обработчика; fetch-ветка сохранена байтово (только поле transport в diag). Тесты: 108/1914/6/0 (+9 X-пинов tests/qwen-o39-history-reasoning.test.js). Finding: core/background.js:169-179 регистрирует MAIN-скрипт программно с run_at:'document_start', world:'MAIN' → хук встаёт до первых XHR (поздняя регистрация не была причиной слепоты). Живая приёмка O-39 scenario 2 (SPA-переход/F5) не выполнена.
  O-39 ЗАКРЫТ живой приёмкой (2026-09-20 18:53–18:56): SPA-открытие bade310b и 7fec9bd3 на сборке с XHR-хуком: консоль histWritePath=network, baseSeen=1, netBaseMsgs=2/73, o22-history-dedup verdict=dedup-skip; файлы …-18-53 и …-18-56 содержат [REASONING]/[ANSWER] (45 совпадений Notepad++ во втором). Бонус: DOM virtual scroll закрыт серверной базой (baseCount=73, pct=50.3 без скролла; ранее 8% → 50%+ после скролла). Наблюдение (решение владельца а/б): строка reasoning виджета на SPA-открытии исчезла — usage истории не содержит reasoning_tokens (измерено 17:00: usage{input_tokens,output_tokens,characters}), ранее строка была stale-значением SSE с маркером stale; netServerTokens=0 при history-записи (usage истории не подключён к serverTokens, не исследовано).
  O-40 (санация): экспорт DeepSeek deepseek-565a7cd8-2026-09-20_11-40.txt содержит инжекции соседнего расширения Better DeepSeek (<BetterDeepSeek>, <BDS:...>); санация O-20 знает только маркеры DeepSeek++; направление фикса: универсальная вырезка инжекций соседей (белый список доверенного контента либо набор паттернов тегов расширений), пины на чистый текст пользователя.
- КОНВЕНЦИИ (дополнить): (1) локальная синхронизация handoff предшествует любой фикс-сессии; коммит блока — в docs-коммите R3; (2) сид фикс-сессии содержит: блок состояния + живой артефакт дефекта + явную инструкцию читать код из workspace/MCP; (3) приёмка включает инспекцию файла экспорта; (4) F5 на чате = новый instance: латч автоэкспорта пуст, первый файр штатен (граница O-38).
- ОЧЕРЕДЬ: O-39 → O-40 (отдельные сессии, WIP=1), затем R1 совмещённая приёмка (остаток: [REASONING] в экспорте thinking-чата; чистота DeepSeek-экспорта от тегов Better DeepSeek; регресс шести платформ точечно) → R2 пакетный коммит → R3 docs (коммит этого блока) → R4 релиз 2.0.11 → R5 GitHub Release → R6 голова очереди O-23.

## Обновление 2026-09-19 (релизная пачка 2.0.10 собрана; тег v2.0.10 на этом docs-коммите)
Перекрывает конфликтующие места блоков ниже (включая строки релиза предыдущих обновлений):
- HEAD: этот docs-коммит (поверх `e080313` release v2.0.10 → `89201e6` fix O-22 → `558e832` docs O-22). Рабочая копия чиста после коммита.
- Релизная пачка 2.0.10: состав 11 трекаемых файлов (manifest/package 2.0.10, CHANGELOG [2.0.10] Fixed с O-14 и O-22, футер docs/index.html, 7 версионных пинов) + 2 untracked на диске (package-lock.json, tools/edge-listing-metadata.txt); сьют 93/1616/6 (число тестов не изменилось).
- Отклонения от промпта релизного бампа: (а) три негативных ассерта version-hygiene переведены с not.toContain('2.0.1') на regex 2\.0\.1(?!\d) из-за префиксной коллизии 2.0.1 внутри 2.0.10 — смысл сохранён (lookahead не матчит 2.0.10), логика пинов не менялась; (б) мок-литерал 2.0.10 в tests/o14-reset-popup-snapshot.test.js:223 оставлен для согласованности (мок песочницы, не пин релизной версии).
- Тег v2.0.10: первоначально создан на `e080313` (отступление от конвенции «тег на верхнем docs-коммите пачки»); переставлен на этот docs-коммит через `git tag -f` + `git push origin -f v2.0.10` (восстановление конвенции).
- GitHub Release v2.0.10: шаг владельца (создать релиз на теге v2.0.10; Release ZIP workflow соберёт asset автоматически).
- Очередь: голова — O-23; далее O-24; O-34 (Low); хвост: O-29 → O-32 → O-21 (малоактуальна).

## Обновление 2026-09-19 (O-22 закрыта в main; голова очереди — O-23)
Перекрывает конфликтующие места блоков ниже (включая строки очереди предыдущих обновлений):
- HEAD: этот docs-коммит (поверх `89201e6` fix O-22 → `27df84a` измерение-2 → `f201957` инструментирование O-22 → `d237c51` docs очереди). Рабочая копия чиста после коммита.
- O-22 ЗАКРЫТА в main: источник F4 = сайт S1172 (deepseek-intercept.js) — тройной диспатч ai-cm-full-history с идентичным payload за ~10 мс на записи нового хода (живой лог 2026-09-18 21:31:12); фикс `89201e6` — payload-точный гард в emitBaseSnapshot (сигнатура по всем полям detail, сброс на смене convId и ai-cm-conversation-changed), 10 R-D пинов tests/adapters/deepseek-o22-dispatch-dedupe.test.js, сьют 93/1616/6.
- Живая приёмка 2026-09-19 11:46–11:51 (DeepSeek, DeepSeek++ выкл, оба гейта ВКЛ, сборка 89201e6): запись нового хода 11:49:05 — 3 MAIN-диспатча S1172 (.318/.323/.324), но ровно 1 svc-emit/badge-recv/listener-entry и 2 o22-dispatch-skip (sig d1aa2b); загрузка 11:48:33 и SPA-смена 11:49:15 — одиночные диспатчи прежние, гард сброшен на смене (write с lastKey=(нет)); SW: все 3 записи win100ms=1. Критерий провала (тройной svc-emit/badge за 4–7 мс) не реализовался.
- Границы: кандидат (b) (deferred-flush) не упражнялся ни в одном из 4 прогонов (ноль строк o22-deferred-flush) — за границей закрытия; маркер o22-call725 печатает src= пусто (дефект aiCmDiagStack(2)) — закрыт как неблокирующий (сопоставление по ts); инструментирование O-22 и измереия-2 остаётся в main под гейтом как источник пинов.
- Очередь: голова — O-23; далее O-24; O-34 (Low); хвост: O-29 → O-32 → O-21 (малоактуальна).
- Релиз: O-14 и O-22 входят в следующую релизную пачку (номер версии и CHANGELOG-секция — только в релизном коммите, конвенция); тег v2.0.9 не переставляется.
- Служебное: в этом же коммите — конвенция «Промпт кодеру» (строка 144): промпт = один блок из четырёх обратных кавычек, вложенные тройные оградки запрещены (инцидент 2026-09-18).

## Обновление 2026-09-18 (очередь: O-21 перенесена в хвост решением владельца; голова — O-22)
Перекрывает конфликтующие места блоков ниже (включая строки очереди предыдущего обновления):
- Решение владельца 2026-09-18: O-21 (F3: усечение DOM-экстрактора DeepSeek msgs=3 при сетевых msgs=6 под соседом DeepSeek++) перенесена в хвост очереди как малоактуальная; живые артефакты F3 не сохранились (владелец удаляет файлы прогонов с Desktop несколько раз в день), механизм — неподтверждённая гипотеза; промпт инспекции O-21 отозван, сессия DSH не заказывалась.
- Открытая очередь: O-22 (голова) → O-23 → O-24; O-34 (Low); хвост: O-29 → O-32 → O-21 (малоактуальна).
- Уточнение конвенции «Окружение кодера»: файлы живых прогонов на `C:\Users\oleg\Desktop` регулярно удаляются владельцем; отсутствие артефактов — дефолтное допущение; DSH начинает поиск с Desktop только при подтверждении владельца, что файлы конкретного прогона живы.
- Голова O-22: спецификации в PROJECT_HANDOFF.md нет (только групповая строка O-21…O-24) — работа начнётся после спецификации от владельца либо решением владельца переходом к следующему пункту со спецификацией (O-34 / O-29).

## Обновление 2026-09-18 (O-14 закрыта в main; голова очереди — O-21)
Перекрывает конфликтующие места блоков ниже (включая предыдущее обновление и «Состояние проекта»):
- HEAD: этот docs-коммит (поверх `2ed8d7f` fix O-14 → `6f7a37e` docs с битой кодировкой → `e2a6acb` test-хотфикс R2 → `377aab9` docs, тег `v2.0.9`). Рабочая копия чиста.
- O-14 ЗАКРЫТА в main: фикс A+B (`2ed8d7f`) — `resetConversationState` удаляет `aiCmState`/`aiCmState:<host>` из `chrome.storage.local`; `options.onChanged` обрабатывает removed-событие per-host → `showNoData` + снятие stale-предупреждения; 10 пинов `tests/o14-reset-popup-snapshot.test.js`; сьют 92/1606/6; CI Lint #25 на `2ed8d7f` зелёный.
- Живая приёмка O-14 2026-09-18 17:57–18:1x (DeepSeek, 4 наблюдения): после SPA-смены чата попап ни разу не показал снимок предыдущего чата (61 414/48% → 149 224/116.6% → 2 725/2.1% → 192 393/150.3%; метка live (сеть/DOM)). Граница: окно «Нет данных» (ветка removed) не поймано в 3 попытках (≤3 с, <1 с, макс. скорость) — сетевой снимок нового чата приходит быстрее открытия попапа; прикрытие — юнит-пины D1/D2×4/D3/R1–R3.
- Инцидент кодировки 16:06 (`6f7a37e`): PowerShell `Get-Content` без `-Encoding` прочитал UTF-8 без BOM как CP1251 и записал кракозябры; `6f7a37e` и `2ed8d7f` несли битый handoff в origin/main (окно 16:06→этот коммит); восстановлено из `e2a6acb` (`git checkout`), история не переписывается. Загрязнены были только main (file-view, `main.zip` по ссылке docs); релизные артефакты v2.0.9 созданы до 16:06 (Release ZIP #12 в 12:00 с дерева `e07cfa4`; source-архивы тега = `377aab9`) — по таймлайну кракозябр не содержат (гипотеза по таймлайну; содержимое asset-zip при желании сверить распаковкой).
- Правило: `PROJECT_HANDOFF.md` не править через `Get-Content`/`Set-Content` без явной кодировки; только Notepad++ или `[IO.File]::ReadAllText/WriteAllText` с явным UTF8.
- Релиз: v2.0.9 опубликован (тег `v2.0.9` на `377aab9`, Release ZIP #12, 2.63 MB); O-14 войдёт в следующую релизную пачку (номер версии и CHANGELOG-секция — только в релизном коммите, конвенция).
- Голова очереди — O-21; далее O-22 → O-23 → O-24; O-34 (Low); O-29/O-32 — хвост.

## Обновление 2026-09-18 (O-7 закрыта, v2.0.9 собрана, перед публикацией)
Перекрывает конфликтующие места блока «Состояние проекта» ниже:
- HEAD: `75d1db1` (релизный бамп v2.0.9) поверх `940d2c4` (docs: O-7 ЗАКРЫТА) поверх `76c2a31` (fix O-7: OFF-зачистка S1–S5); локально ahead 4 от `origin/main` (`1d3de6c`, тег `v2.0.8`; релиз опубликован: Release ZIP #11, zip 2.62 MB).
- Тесты: 91 suites, 1596 passed, 6 skipped, 0 failed (было 90/1535/6; +12 пинов O-7 S3/S4/S5; релизный бамп число тестов не менял).
- Рабочая копия чиста после релизного коммита `75d1db1` (10 трекаемых файлов: CHANGELOG, manifest, package, docs/index.html + 6 тестовых пинов; package-lock.json и tools/edge-listing-metadata.txt обновлены на диске, gitignored).
- O-7 ЗАКРЫТА в v2.0.9: тумблер `aiCmIncludeHiddenInExport` (default OFF) + OFF-зачистка S1–S5 в `utils/export-emit-pipeline.js` (`isToolResultsOnlyText`/`stripToolCallBlocks`/`isEmptyExportText`); ON байтово прежний; база/метрики/бейдж от тумблера не зависят. Живая приёмка 2026-09-18: txt-прогон 09:37–09:38 — OFF-файлы 0 совпадений findstr по 5 маркерам (`[TOOL_RESULTS]`, `Continue answering…`, `<browser_`, `[REASONING]`, `deepseek-pp-visible-user-prompt`), ON — совпадения есть; тот же DeepSeek++-чат в прогоне 09:00 (до фикса) давал 44 совпадения в OFF → переход 44→0 при неизменной базе; md-прогон 09:52–09:54 — шапки OFF=ON по tokens/limit/percent (61414/128000 (48%); 149224/128000 (116.6%)), различие только в дате экспорта.
- Список тегов S4 уточнён по живым артефактам относительно спецификации: добавлен call-тег `browser_wait_for` (в спецификации отсутствовал); `browser_wait_for_result` — result-тег (живёт внутри `[TOOL_RESULTS]`, уходит вместе с S3); полный список browser call-тегов — 18 (ON-артефакт, строки 127–311). Теги `local_file_read/stat/write`, `local_folder_pick`, `local_skill_preview`, `memory_import_preview` парами в артефактах не встречены — список НЕ расширен (отложенный пункт).
- package-lock.json НЕ отслеживается git (`git ls-files` пуст; строка в `.gitignore`): пин R-1 version-hygiene читает лок с диска; релизный коммит v2.0.9 включает `manifest.json`/`package.json`/`CHANGELOG.md` и версионные пины, лок обновляется только на диске. Формулировка «манифест/пакет/лок» прежних релизов уточнена соответственно (когда лок стал untracked — не установлено).
- Голова очереди — O-14; далее O-21…O-24; O-34 (Low) — позиция не менялась; O-29/O-32 — хвост.
- Следующие шаги: этот docs-коммит (тег `v2.0.9` на нём) → push origin main + push тега v2.0.9 → GitHub Release v2.0.9 (шаг владельца).

## Состояние проекта

**Версия:** 2.0.8 (в `manifest.json`/`package.json`; релизный бамп — коммит `b42a269`)  
**HEAD:** этот docs-коммит (поверх релизного `b42a269` — O-33, версия 2.0.8; docs `deb64cd`; фикс `c1fecb8`); локально **ahead 3** от `origin/main` (`ae35785`); тег `v2.0.7` остаётся на `ae35785`, тег `v2.0.8` — на этом docs-коммите; `push origin main` + push тега `v2.0.8` + GitHub Release v2.0.8 — **следующим шагом владельца**  
**Тесты:** 90 suites, 1535 passed, 6 skipped, 0 failed (было 89/1511/6; +набор O-33, 24 R-D пина; релизный коммит `b42a269` число тестов не менял — только версионные пины)  
**Рабочая копия:** чиста (код/тесты не тронуты; правится только этот handoff — docs); O-11 (раунд 1 `3605375`, раунд 2 `07c83a5`), O-31 (`deafd59`), O-27 (`d11f3d6` + защитный фикс `4ee6eee`) и диагностика (`270927a`) — в `origin/main`; релизная пачка v2.0.7 (`7ea8273` фикс O-1 → `608f744` docs → `52bd2cc` релизный бамп → `ae35785` docs) — опубликована (тег `v2.0.7` на `ae35785`); релизная пачка v2.0.8 (`c1fecb8` фикс O-33 → `deb64cd` docs → `b42a269` релизный бамп → этот docs-коммит) — локально (ahead 3, не запушена); следующим шагом владельца — push origin main + тег `v2.0.8` + GitHub Release, далее — O-7 (голова очереди)  
**Статус:** последний опубликованный релиз — v2.0.7 (тег `v2.0.7` на `ae35785`, GitHub Release); **v2.0.8 готов к публикации** — релизный коммит `b42a269` (`manifest.json`/`package.json`/`package-lock.json` 2.0.8, CHANGELOG [2.0.8], версионные пины) + этот docs-коммит; тег `v2.0.8` — на этом docs-коммите; push и GitHub Release — шаг владельца сразу после этого коммита. O-11 и O-31 ЗАКРЫТЫ живой приёмкой 15:58; **O-27 ЗАКРЫТ** (защитный фикс `4ee6eee` + live-приёмка 19:48: здоровый чат экспортируется байтово как прежде; живая captcha не воспроизводима по требованию — граница зафиксирована). **O-1 ЗАКРЫТ** в v2.0.7 (`7ea8273` + живая приёмка 10:21–10:22: щит бейджа ChatGPT взводится на старте документа независимо от convId, отпускание по `baseSeen`/фолбэк 10 c; границы — форма armed-empty-convid живьём не воспроизвелась, серия оборвана лимитом платформы); **O-33 ЗАКРЫТ в v2.0.8** (`c1fecb8`; релизный коммит `b42a269`) — живая приёмка 2026-09-17 15:33–15:42: файра автоэкспорта по DOM-базе (`baseSeen=false`) нет на 2×F5 и realtime, файр только после `baseSeen=1`, двойных файров нет (`already-fired`); **граница:** `reason base-pending` вживую не наблюдался — нужен DOM pct ≥ порога, при минимальном пороге 1% не достигается; граница зафиксирована по прецеденту O-27, прикрытие — юнит-контур 24 R-D пина. О-1 в `CHANGELOG.md` НЕ заводился: **секция CHANGELOG [2.0.7] — в релизном коммите `52bd2cc`** (конвенция ниже). **O-32 — НИЗКИЙ ПРИОРИТЕТ** (механизм индикатора/процента не воспроизведён в 5+ прогонах, `270927a` — только инструментирование); правило R-D внесено в конвенции процесса. O-15…O-20, O-26, O-9-подпись, O-28 (по дизайну) и O-30 (объяснён путём записи базы) закрыты; **O-7 ЗАКРЫТА** (`dab44f8` + `76c2a31`, живая приёмка 2026-09-18 09:37–09:54); **голова очереди — O-14** (O-34 — Low, дорисовка остаточных значений на URL без `/c/<id>` без сброса; позиция не менялась), далее O-21…O-24; O-29/O-32 — в хвосте. Дистрибуция — GitHub Releases + секция «Ручная установка»; Edge Add-ons отложены бессрочно (комплаенс-блок RU-аккаунта, reference 715-123225)  

## Ключевые достижения

### Функционал
- **6 адаптеров** с единым контрактом (`BaseAdapter`)
- **Автоэкспорт** при достижении порога (все 6 платформ)
- **Ручной экспорт** в 4 форматах (md/json/pdf/txt)
- **Проактивные пороги** 70/85/95% с Chrome-уведомлениями
- **Импорт архивов** (Gemini Takeout, ChatGPT conversations.json, Perplexity, Claude)
- **BYOK** (Google AI Studio API key) для точного подсчёта токенов
- **Санация экспорта** (O-20): вырезание инъекций соседних расширений (DeepSeek++ memory/toolSchema) из текста экспорта без влияния на базу и метрики

### Архитектура
- **Strategy pattern** для адаптеров
- **Чистые функции** в `utils/` (export-emit-pipeline.js)
- **UMD-паттерн** (работает в браузере и Node.js тестах)
- **MV3-совместимость** (нет eval, нет удалённого кода, минимум permissions)
- **Traceability** (комментарии с версиями: v81, M-11, E-2, T1-fix#3)

### Декомпозиция v2.0 — все 3 этапа приняты

**Этап 1/3 (`370a217`):** `content.js` 3622 → 1897 строк, 5 модулей:
- `core/state.js` (416 строк) — глобальное состояние
- `core/widget.js` (510 строк) — виджет
- `core/base-handler.js` (218 строк) — обработка базы
- `core/hybrid-tail.js` (162 строки) — гибридный хвост DOM
- `core/export-manager.js` (~75 KB) — экспорт

**Этап 2/3 (`994ae26`):** `gemini-intercept.js` 5064 → 4800 строк, вынесен:
- `core/gemini-hidden-scroll.js` (356 строк, 13 функций + 10 констант)

Кластеры network/parser/pagination/loader неотделимы (AST-замыкание 84–95%, 218/259 общих имён) — оставлены в ядре до бандлера.

**Этап 3/3 (`be1a4a6`):** миграция id регистрации `ai-cm-gemini-intercept-v2` + `unregisterContentScripts` старого. ES modules для content scripts отложены до бандлера (`ARCHITECTURE_STANDARDS.md`).

## Критические проблемы (требуют рефакторинга)

### God-objects (senior-аудит 2026-09-13)

| Файл | Размер | Проблема |
|---|---|---|
| `core/gemini-intercept.js` | ~330 KB | Перегружен: перехват + парсинг + пагинация + loader + hidden scroll + DR dedup |
| `core/content.js` | ~230 KB (до декомпозиции) | ✅ Декомпозирован на 5 модулей (этап 1/3) |

## Пост-релизная очередь low

| № | Проблема | Статус |
|---|---|---|
| O-15 | DeepSeek live-экспорт: потеря ходов и усечение 2 символов | ✅ ЗАКРЫТ (`c77fb90`) |
| O-16 | Усечение ответа при автоэкспорте во время стрима | ✅ ЗАКРЫТ (`a094920`) |
| O-17 | SPA-созданный чат: baseComplete/turnsMap неполны | ✅ ЗАКРЫТ (`07f45ce`) |
| O-18 | DeepSeek: рассинхронизация SSE-парсера + неполнота при экспорте | ✅ ЗАКРЫТ (`072ae6a`) |
| O-20 | Санация инъекций соседних расширений (DeepSeek++ memory/toolSchema) | ✅ ЗАКРЫТ (`f71f7d1`) — user-текст обрезается до видимого между парой маркеров `deepseek-pp-visible-user-prompt`; tokens/percent не пересчитываются |
| O-19 | Приёмочный прогон с включёнными соседними расширениями | ✅ ЗАКРЫТ — расхождения объясняются инъекциями соседа; **продуктовое решение:** инъекции соседа и его тул-коллы остаются в серверной истории, расширение их не удаляет |
| O-25 | Пункт пост-релизной очереди low | ❌ ОТКЛОНЕНА (решение аудита) |
| O-26 | Диагностика походового usage DeepSeek (рост бейджа на длинном чате) | ✅ ЗАКРЫТ (`1e0bb14`) — вербатим-дамп usage по ходам + счётчики netMsgs/domMsgs в svc-emit-trace под гейтом `aiCmDebug`; поведение не меняется |
| O-9-подпись | `snapshot-at-manual` печатает `msgs=0` на Perplexity | ✅ ЗАКРЫТ (`59a295f`) — подпись берёт реальные ходы и тексты из канонической базы (`buildHistoryMessages`); live-приёмка: Perplexity `msgs=4` при эталоне 4; мост MAIN (Gemini/DeepSeek O-17) остаётся приоритетным |
| O-11 | Две маски имён автоэкспорта + коллизия на GSA | ✅ ЗАКРЫТ (`3605375` + раунд 2 `07c83a5`) — имя автоэкспорта строит РОВНО ОДНА функция-источник `buildAutoExportFileName` (обе прежние точки — тонкие обёртки); гард коллизии: занятое имя получает дисамбигуатор `-2`, `-3` …; **раунд 2:** `aiCmAutoExportStartDownload` — единственная точка старта скачивания автоэкспорта для ВСЕХ сайтов: реестр имён консультируется и имя резервируется СИНХРОННО **ДО** старта скачивания (раньше — после `downloadBlob`); обход реестра GSA-путём **опровергнут** read-only инспекцией (единственный `downloadBlob` автоэкспорта — в этой точке; GSA-ветка имени входит в неё); сьют-пин живого сценария: fire-путь дважды с разными convId в одну минуту → второе имя = первое + `-2`; шаблон ручного экспорта и маска байтово прежние (пин M-10/F4). **Живая приёмка (12:15 → 15:58):** была заблокирована до O-31 (чужой хвост в базе делал состав файла недостоверным); **ПРОЙДЕНА в прогоне 15:58 после фикса `deafd59`** — два файра в одну минуту → `…-15-58.txt` и `…-15-58-2.txt` (второе имя с нашим `-2`, chrome-овского ` (1)` нет), базы чистые per-conversation; граница: реестр — на страницу (ISOLATED-мир), приёмка выполнена после перезагрузки расширения |
| O-27 | GSA: captcha/«подозрительный трафик» после пагинационного дозапроса folwr | ✅ **ЗАКРЫТ** (`d11f3d6` + защитный фикс `4ee6eee`; live-приёмка 19:48 — здоровый чат экспортируется байтово как прежде) — живой прогон 10:46: на сервисной странице Google (captcha) автоэкспорт скачал мусор `)]}' [""]` (файл `f.txt`). Фикс `d11f3d6`: (а) сетевой писатель GSA (`applyTurns`/`mergeTurns`) требует ≥1 НЕПУСТОЙ ход; (б) гейт автоэкспорта (`shouldSkipGsaPageGuard`) требует `msgs≥1`, непустой текст базы и признак чат-страницы — причины `not-chat-page`/`no-messages`/`empty-base`; (в) `aiCmGsaChatPageMarker` + `aiCmGsaResetStaleStateOnChatlessDoc`. **Защитный фикс `4ee6eee` (по живой форме мусора):** гард СУЖЕН до единственной известной формы — `isUsableTurn` больше не режет ход по тексту, форма мусора решается ТЕЛОМ (`isGarbageBody`: тело начинается XSSI-префиксом `)]}'` И разбор дал 0 непустых ходов; на легитимном теле из логов 18:47 `shape=turns=5 empty=0`, `validation=hasUsableTurns=true` — гард не отклоняет ничего); пост-гард в ЕДИНСТВЕННОЙ точке скачивания (`downloadBlob`, через неё и `aiCmAutoExportStartDownload`) — файл не выдаётся и пишется `[AI CM][diag] download-blocked reason=xssi-prefix|empty-name`, если первые байты контента начинаются `)]}'` ИЛИ имя файла пусто/не задано (закрыт путь пустого имени независимо от триггера). **Юнит-приёмка:** `tests/gsa-o27-captcha-base-guard.test.js` + `tests/gsa-o27-protective-form-guard.test.js` (R-D: каждый путь записи базы + точка скачивания: легитимное тело → база и txt-экспорт байтово прежние; XSSI-тело → база не пишется, baseComplete не взводится, download blocked reason=xssi-prefix; пустое имя → blocked reason=empty-name; регресс O-11/O-31/O-15…O-20; диаг-строки только под гейтом). Сьют **88/1495/6**. **Граница:** живая captcha не воспроизводима по требованию (Google отдаёт её по своей bot-detection) — живой приёмкой закрыт только юнит-контур; воспроизведение — при следующем естественном срабатывании. Пороги на чат-страницах, байты/маска экспорта, латч и реестр имён (O-11) не тронуты. `solveSimpleChallenge is not defined` — скрипт Google, не наш код |
| O-28 | Молчание автоэкспорта GSA до дозревания базы | ✅ ЗАКРЫТ (поведение по дизайну) — гейт `baseComplete`; задержка = дозревание базы GSA (DOM pagination-empty → network/memory), файл только при `baseComplete=1` |
| O-29 | Лог-спам автоэкспорта GSA (`latch kept reason=spa-entry` / `skip already-fired\|not-complete` каждую секунду) | ✅ **ЗАКРЫТ живой приёмкой 2026-09-22:** антиспам-гард `aiCmAutoExportLastLogState` (family→сигнатура) + хелперы `aiCmAutoExportLogOnChange`/`aiCmAutoExportLogFiredBit`; лог только на смену состояния; сьют 117/2096/6/0 (+24 пина). Наблюдение: race condition memory-snapshot vs network-finalize (дельта 262 мс) — вне scope, требует инструментирования. |
| O-30 | Нестабильность состава экспорта одного и того же чата GSA (DOM vs memory/network) | ✅ ЗАКРЫТ (`deafd59`) — объяснён ПУТЁМ ЗАПИСИ БАЗЫ: в файл шло не «DOM vs memory», а склейка базы двух разговоров (поздний ответ старого треда дописывался в активную базу), поэтому в файле B появлялись/исчезали user-строки; после сегментации O-31 состав канонический per-conversation (пин tests/gsa-o31-thread-segmentation.test.js) |
| O-31 | GSA: сетевая база дописывает ходы чужих разговоров при SPA-переключении (`netMsgs` 8→10→18→20, файл B с хвостом A) | ✅ ЗАКРЫТ (`deafd59`) — база и счётчики сегментированы по разговору (threadId): смена треда сбрасывает сетевой накопитель, снимок чужого треда пишется только в свой сегмент и не эмитится; content.js отбрасывает чужой снимок до присвоений базы. **Живая приёмка 15:58:** `…-15-58.txt` / `…-15-58-2.txt` — msgs=8/2, textLen=5121/13270, pct=1.4/3.5, базы чистые per-conversation |
| O-43 | GSA: race condition автоэкспорта — файл по memory-snapshot до дозревания network-базы | ✅ **ЗАКРЫТ живой приёмкой 2026-09-23** — файл по полной сетевой базе (netLatch=true, msgs=54, textLen=56884), обрыва истории нет; сьют 118/2117/6/0; монотонный латч сетевой полноты `aiCmGsaNetworkCompleteLatch` (пины tests/o43-gsa-autoexport-race.test.js) |
| O-45 | GSA: код-блоки (окна javascript/python) не попадают в экспорт txt/md | ✅ **ЗАКРЫТ живой приёмкой 2026-09-23** — экспорт содержит полный текст код-блоков с маркером языка (python/javascript; файлы 11-05/11-08); сьют 119/2143/6/0; фикс `utils/google-search-folwr-parser.js` (селекторы + хелпер `renderCodeBlock`) + адаптер переиспользует хелпер; 26 пинов `tests/o45-gsa-code-blocks-export.test.js`. Наблюдение: fence-бактикы (```) в txt-экспорте не наблюдаются — код идёт строкой-меткой + телом; сверка с пинами D1/D6 перед коммитом фикса |
| O-46 | manifest: добавить `utils/google-search-folwr-parser.js` в `content_scripts[].js` | ✅ **ЗАКРЫТ 2026-09-23 (коммит d3d0ea0 поверх 7d66180):** в `content_scripts[0].js` манифеста добавлена строка `utils/google-search-folwr-parser.js` (сразу после `utils/gemini-batchexecute-parser.js`; ровно +1 строка, версия 2.0.11 не менялась) — единство DOM/сетевых путей без фолбэка адаптера, `window.GoogleFolwrUtils` виден и в ISOLATED-мире адаптера; сьют 120/2167/6/0 |
| O-32 | Механизм индикатора/процента GSA (инфляция токенов после SPA) | ✅ **ЗАКРЫТ живой приёмкой 2026-09-23:** инвариант канонической идентичности (hash=0f9f7baa/32e4a45b стабилен при F5/SPA); сьют 120/2167/6/0 (+23 пина) |
| O-33 | Автоэкспорт по DOM-базе до сетевого снимка (ChatGPT) | ✅ **ЗАКРЫТ в v2.0.8** (`b42a269`; фикс `c1fecb8`) — блок автоэкспорта по DOM-базе до сетевого снимка (`reason base-pending` после порогов; лог только на смене состояния), 24 R-D пина. **Живая приёмка 2026-09-17 15:33–15:42:** файра по DOM-базе (`baseSeen=false`) нет на 2×F5 и realtime, файр только после `baseSeen=1`, двойных файров нет (`already-fired`). **Граница:** `reason base-pending` вживую не наблюдался — нужен DOM pct ≥ порога, при минимальном пороге 1% не достигается (прецедент O-27) |
| O-34 | Наблюдение N1: дорисовка остаточных значений на URL без `/c/<id>` без сброса | **Закрыт 2026-09-22** живой приёмкой (коммит 239ac61, 116/2072/6/0); голова очереди — O-29 (Low). |
| O-1 | DOM-мисдетект ChatGPT ~1 c после F5 | ✅ **ЗАКРЫТ в v2.0.7** (`7ea8273` + приёмка 10:21–10:22) — щит бейджа взводится на старте документа независимо от convId, отпускание по `baseSeen`/фолбэк 10 c, плейсхолдер P2a, 16 R-D пинов (`tests/chatgpt-o1-badge-hold.test.js`). **Границы:** форма armed-empty-convid живьём не воспроизвелась, серия оборвана лимитом платформы. **Секция CHANGELOG [2.0.7] — в релизном коммите `52bd2cc`** |
| O-7 | Экспорт DeepSeek без reasoning-цепочек | ✅ **ЗАКРЫТ в v2.0.9** (`dab44f8` тумблер + `76c2a31` зачистка S1–S5 + `75d1db1` релизный бамп; живая приёмка 2026-09-18 09:37–09:54) |
| O-14 | Попап держит старый снимок после RESET | **Открыт (Low):** косметика попапа |
| O-21…O-24 | Пункты пост-релизной очереди low (`O-21`, `O-22`, `O-23`, `O-24`) | **O-21 — Открыт (голова очереди, Low);** O-22 **ЗАКРЫТ в main** (`89201e6`, payload-точный гард повторного диспатча DeepSeek); O-23 **ЗАКРЫТ как expected behaviour 2026-09-22** (несовпадение базиса счёта, а не ошибка коэффициента; код в `924e878`); O-24 **ЗАКРЫТ живой приёмкой 2026-09-22** (сигнатурный гард svc-emit после already-fired) |

**Открытая очередь:** O-21 (голова, Low); далее O-14 (косметика попапа, Low) — см. таблицу низкого приоритета. Закрыты и из очереди сняты: O-22 (`89201e6`), O-23 (expected behaviour 2026-09-22, `924e878`), O-24 (живая приёмка 2026-09-22, `02d60c9`), O-34 (живая приёмка 2026-09-22, `239ac61`), O-46 (`d3d0ea0`).

**Платформенная заметка:** GSA умеет урезать историю сервером. Расширение считает то, что сервер реально отдал.

**Платформенная заметка (O-27):** пагинационные folwr-дозапросы GSA (raw ~1.2 МБ на страницу, до 12 страниц за одно открытие + probe-шаги) выглядят для Google ботоподобно: реакция — captcha / «подозрительный трафик» (/sorry), после чего Google отдаёт на async-запросы пустой payload. В живых прогонах учитывать: длинную историю дособирать порциями, между прогонами давать паузу, при появлении captcha — прекращать прогон (circuit-breaker `429/sorry` уже глушит свои запросы на 10 мин), а файлы автоэкспорта после прогона проверять на мусор (тело с XSSI-префиксом `)]}'` и пустым payload в базу больше не попадает — `d11f3d6`; форма мусора и пустое имя файла отсекаются ещё и в единственной точке скачивания — `4ee6eee`).

## Технические детали

### Структура проекта
```
ai-context-monitor-clean/
├── manifest.json (MV3, version 2.0.8)
├── core/
│   ├── content.js (1897 строк) ✅ декомпозирован (этап 1/3)
│   ├── state.js, widget.js, base-handler.js, hybrid-tail.js, export-manager.js ✅ (O-11 раунд 2: единственная точка старта скачивания автоэкспорта `aiCmAutoExportStartDownload`)
│   ├── background.js
│   ├── gemini-intercept.js (~4800 строк) — ядро
│   ├── gemini-hidden-scroll.js (356 строк) ✅ вынесен (этап 2/3)
│   ├── claude-intercept.js, deepseek-intercept.js, google-search-intercept.js
│   ├── perplexity-intercept.js, page-intercept.js
├── adapters/ (base-adapter.js + 6 адаптеров)
├── utils/
│   ├── export-emit-pipeline.js (~74 KB, чистые функции + санация O-20 + единый источник имён автоэкспорта и гард коллизии O-11)
│   ├── gemini-intercept-logic.js, gemini-batchexecute-parser.js
│   ├── google-search-folwr-parser.js, archive-import.js
├── options/options.js
├── tests/ (91 suites)
├── CHANGELOG.md, RELEASE_CHECKLIST.md
└── tools/edge-listing-metadata.txt (на будущее)
```

### Конвенции

**Кодер:** DSH Desktop (DeepSeek Harness), workspace `ai-context-monitor-clean`  
**Модель:** deepseek-flash  
**Промпт кодеру:** режим act; контекст-перенос ≤3 строк; задача пунктами; список «не трогать»; ответ = diff + отчёт ≤10 строк. Оформление: промпт = ОДИН блок, огороженный четырьмя обратными кавычками; внутри промпта тройные оградки запрещены — коммит-сообщения и код приводятся обычными строками (инцидент 2026-09-18: вложенные тройные оградки разрушили блок промпта O-22, коммит-строка выпала из блока). 
**§0:** первая строка контекст-переноса всегда: Принцип качества: §0 handoff (стандарты + senior-уровень)  

**Усилие рассуждений:** обязательный параметр в каждом промпте для DSH.  
- **Max** — архитектурные задачи (рефакторинг, декомпозиция, миграции)  
- **Medium** — фиксы (O-9-подпись, O-11, O-19)  
- **Low** — косметика (O-14, комментарии)  
- **Формат:** первая строка промпта, перед `Режим: act`

**Тарификация DSH:** peak/off-peak (пик Пекин 09:00–12:00 и 14:00–18:00 = Екб 06:00–09:00 и 11:00–15:00; тяжёлые прогоны — вне пика)

## Конвенции процесса

Правила, выработанные в цикле O-15…O-20:

1. **Два провала фикса одного симптома → следующий промпт только измерение.** Если симптом воспроизводится после второй итерации, следующая сессия заказывает измерение без правок.
2. **Каждое утверждение с основанием.** Все утверждения помечены: «лог строки N», «скриншот», «гипотеза, не проверено».
3. **Критерий приёмки привязан ко времени.** «Снимки в пределах одной секунды» или «одновременно».
4. **Харнесс ≠ приёмка live-багов.** Приёмка = живое побайтовое равенство на захваченном payload в реальном браузере.
5. **Правило R-D (regression + defect-path).** Каждый защитный фикс по дефекту закрывается пинами на КАЖДЫЙ путь записи (base write path) и на единственную точку выхода — а не на «представительный» путь:
   - **D (defect)** — пин на воспроизведение дефекта: точная форма входных данных из живого артефакта (для O-27 — `f.txt` = `)]}'\n[""]\n`) на ВСЕХ путях записи базы (`open`, пагинация, `probe` step/finish, passive `folwr`/`folif`, XHR, DOM-добор) и в единственной точке скачивания (`downloadBlob` → `aiCmAutoExportStartDownload`);
   - **R (regression)** — пин на легитимный вход: база и экспорт байтово прежние (сравнение снимка и байтов файла «до/после» гарда), плюс прогон существующих наборов (O-11/O-31/O-15…O-20) в том же сьюте;
   - плюс пин «диагностика только под гейтом» (`aiCmDebug`) и «байты выхода при гейте вкл/выкл идентичны».
   Живой образец: `tests/gsa-o27-protective-form-guard.test.js` (16 пинов).

6. **Секция CHANGELOG создаётся только в релизном коммите** одновременно с бампом версии (`manifest.json`/`package.json`/`package-lock.json`) и версионными пинами; до релизного решения CHANGELOG не трогается. Страж — `tests/version-hygiene.test.js` (`manifest.version ===` версия ВЕРХНЕЙ записи CHANGELOG). Так оформлены секции [2.0.4]/[2.0.5]/[2.0.6]; секция [2.0.7] (O-1) — в релизном коммите `52bd2cc`; секция [2.0.8] (O-33) — в релизном коммите `b42a269`.

## Окружение кодера

- **DSH Desktop (anywhere-labs)** стабилен на чистом наборе плагинов.
- **Лаунчер:** `C:\DSH\DSH.vbs` + `DSH.ps1` — Edge app mode, изолированный профиль `DSH-Edge-Profile`.
- **Плагины:** `dshmarket`, `dsh-api-cost`, `dsh-reasoning-effort`.
- **Правило:** установка плагинов — только при полностью остановленном DSH.
- **temperature:** обсуждение опубликовано в `deepseek-ai/deepseek-harness`.
- **vision:** DSH не передаёт изображения модели — нужен `dsh-vision-bridge` или ждать upstream.
- Файлы экспорта живых прогонов лежат на `C:\Users\oleg\Desktop` — DSH начинает поиск оттуда.

### Приёмочная матрица (v2.0.8 + O-9-подпись/O-20/O-26/O-11/O-31/O-27/O-1/O-33)

| Критерий | Статус |
|---|---|
| Ядро 6 адаптеров (виджет=бейдж=лог, модель из сети, полная база) | ✅ |
| Автоэкспорт (все 6 платформ) | ✅ |
| Четыре формата из одного чата (md/json/pdf/txt) | ✅ |
| Ru-локаль (options/privacy/docs) | ✅ |
| Тесты 85/85, 1439 passed, 6 skipped, 0 failed → **90/90, 1535 passed, 6 skipped, 0 failed** | ✅ |
| Версия 2.0.8 во всех точках вывода | ✅ (футер `docs/index.html` + листинг Edge — сверить по релизному коммиту `b42a269`; иных точек вывода нет по grep релизной сессии) |
| GitHub Release v2.0.8 — релизный коммит `b42a269` + этот docs; тег `v2.0.8` на этом docs-коммите; push и публикация — шаг владельца сразу после коммита | ⏳ |
| O-15…O-20, O-26, O-9-подпись live-приёмка | ✅ |
| O-11: два автоэкспорта GSA в одну минуту → два РАЗНЫХ имени, обе копии целы (сьют-пины `3605375`) | ✅ |
| O-11 раунд 2: единственная точка старта скачивания (`aiCmAutoExportStartDownload`), синхронный резерв имени ДО старта, обход GSA опровергнут (сьют-пины `07c83a5`) | ✅ |
| O-31: сегментация сетевой базы GSA по разговору — два разговора эмитят последовательно → базы 8 и 2 (НЕ 10), pct/экспорт по текущему (сьют-пины `deafd59`) | ✅ |
| O-30: канонический per-conversation состав экспорта — user-строки своего разговора на месте, чужих ходов нет | ✅ |
| O-11 + O-31 живая приёмка 15:58 «два файра за минуту → `-2`» | ✅ ЗАКРЫТА: файлы `…-15-58.txt` и `…-15-58-2.txt` (msgs=8/2, textLen=5121/13270, pct=1.4/3.5), базы чистые per-conversation, chrome-суффикса ` (1)` нет |
| O-27 защитный фикс по форме мусора (`4ee6eee`): гард базы сужен до `)]}'`+0 ходов, пост-гард точки скачивания (пустое имя / xssi-prefix) | ✅ ЗАКРЫТ: сьют-пины R-D (`tests/gsa-o27-protective-form-guard.test.js`, 16) + `tests/gsa-o27-captcha-base-guard.test.js`; live-приёмка 19:48 — здоровый чат экспортируется байтово как прежде; живая captcha не воспроизводима по требованию |
| O-1 (бейдж ChatGPT после F5): щит взводится на старте документа независимо от convId, отпускание по `baseSeen`/фолбэк 10 c, плейсхолдер P2a | ✅ **ЗАКРЫТ в v2.0.7** (`7ea8273` + приёмка 10:21–10:22): живая приёмка 10:21–10:22 + 16 R-D пинов (`tests/chatgpt-o1-badge-hold.test.js`, сьют 89/1511/6); границы — armed-empty-convid живьём не воспроизвелась, серия оборвана лимитом платформы |
| O-33: нет файра автоэкспорта по DOM-базе (live 15:33–15:42: 2×F5 + realtime, файр только после baseSeen=1) | ✅ **ЗАКРЫТ в v2.0.8** (`b42a269`; фикс `c1fecb8`): 24 R-D пина, сьют 90/1535/6; граница — `reason base-pending` вживую не воспроизведён (DOM pct < порога 1%), прецедент O-27 |
| O-32 (механизм индикатора/процента GSA) | ⏳ **ОТКРЫТ (Medium):** инфляция токенов после SPA-возврата (4.4% → 9.0% без новых сообщений); требует локализации рассогласования netMsgs vs domMsgs |

## GSA Live Run 2026-09-16 — verdicts и наблюдения (O-11 round 1, O-27, O-28, O-29, O-30)

**Параметры прогона:** порог 1%, формат txt, два тестовых чата (A = вставленный blob из экспорта 10-37; B = оригинал с user/assistant), SPA-переключение между ними, консоль под гейтом `aiCmDebug`.

- **O-11 round 1: живой провал гарда (факты).** Файр №1 в 10:49:05.823 (`convId=3yyqar…`, `textLen=13270`, `baseComplete=1`, `histSource=memory`) и файр №2 ~10:49:23 (`convId=kSqqasn…`; вывод о скачивании — по строке `skip reason=already-fired` в 10:49:23.711 и наличию второго файла) дали одно имя `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-10-49.txt`; второй файл получил суффикс ` (1)` от Chrome, а не `-2` от нашего гарда. Разрыв ~18 с исключает гонку реестра.
- **O-11 round 2: инспекция всех точек скачивания — обход ОПРОВЕРГНУТ.** Единственная точка скачивания автоэкспорта — `core/export-manager.js:798` (`Bdl.downloadBlob`) внутри `aiCmAutoExportStartDownload` (функция с 770), вызываемой ровно один раз из `doAutoExportDownload` (`core/export-manager.js:1017`); GSA-ветка имени `core/export-manager.js:868` (`buildGsaExportFileName(..., namesUsedD)`) входит туда же через `maybeAutoExport → doAutoExportDownload` (порог/`base-complete`/`pre-trim`). Второй источник скачивания в `core/` — только ручной дамп `Ctrl+Shift+D` (`core/content.js:1892–1898`, не автоэкспорт); в `core/google-search-intercept.js` скачивания нет вообще (строка 224 — диспатч `ai-cm-full-history`). Строки `fired` печатает `core/export-manager.js:1018` (общая) и `:1025` (GSA-tagged), а не перехватчик. **Фикс раунда 2 (`07c83a5`):** реестр имён консультируется и имя резервируется СИНХРОННО ДО старта скачивания (раньше — после); сьют-пины: fire-путь дважды с разными convId в одну минуту → второе имя = первое + `-2`, повторный вход ИЗ `downloadBlob` → `-2`. **Причина живого провала (гипотеза, не проверено):** сборка в браузере без гарда раунда 1 — `3605375` идёт ПОСЛЕ релизного коммита `a7f73a8` (v2.0.4), поэтому релизный zip/распакованная копия гарда не содержит; лечится перезагрузкой расширения и повторным живым прогоном. Альтернатива — разные контексты страницы (реестр живёт в ISOLATED-мире страницы), при SPA-переключении одного таба не применима.
- **Уточнение вреда O-11:** в Chrome нет потери байтов — браузер сам уникуфицирует имена через ` (1)`. Вред = нарушение маски и непредсказуемость имён; поведение в других браузерах не проверено.
- **O-28 закрыт (поведение по дизайну):** молчание автоэкспорта до дозревания базы — гейт `baseComplete` (строка `skip reason=not-complete convId=kSqqasn… pct=1.4` печатается каждую секунду с 10:48:54 до 10:49:22). Особенности GSA: DOM-путь pagination-empty (`virtual scroll: container not found`), база дозревает через network/memory; файр — только при `baseComplete=1`.
- **SPA-переключение запускает автоэкспорт по разговору:** строка `latch kept reason=spa-entry convId=(threadId)` — латч сидит на разговоре (threadId), а не на вкладке.
- **O-27 открыт (Medium-High) → ЗАКРЫТ (`d11f3d6`, см. раздел ниже):** в 10:46:54Z Google выдал captcha / «подозрительный трафик» (вероятная реакция на пагинационный дозапрос folwr, raw 1 195 742 B — выглядит ботоподобно); на этой странице автоэкспорт сработал и скачал мусор `)]}' [""]` (содержимое файла `f.txt`). `ReferenceError: solveSimpleChallenge is not defined` — собственный captcha-скрипт Google, не наш код.
- **O-29 открыт (Low):** лог-спам — пара `latch kept reason=spa-entry` / `skip already-fired|not-complete` печатается каждую секунду непрерывно; нужно логировать только на смене состояния.
- **O-30 открыт (Low, гипотеза):** нестабильность состава экспорта одного и того же чата B: файл 10-37 без user-строк, файл 10-49 (1) с user-строками; гипотеза — разный источник базы (DOM vs memory/network). Требует измерения.
- **Синхронизация таблицы очереди — выполнена (docs-коммит DSH раунда 2):** O-11 статус post-fix + строки O-27/O-28/O-29/O-30 в таблице, O-28 закрыт как поведение по дизайну, O-29 открыт (Low).

**Цитаты (файлы на `C:\Users\oleg\Desktop`):**
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-10-49.txt` (разговор A, file-#1)
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-10-49 (1).txt` (разговор B, file-#2, Chrome-суффикс)
- `f.txt` (мусор с captcha-страницы: `)]}'` + перевод строки + `[""]` + перевод строки = точная форма мусора защитного фикса `4ee6eee`)
- `Логи..txt` (лог прогона 18:47 под гейтом `aiCmDebug`: доказательство `shape=turns=5 empty=0`/`hasUsableTurns=true` на здоровом пути и отсутствие воспроизведения O-32)
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-10-37.txt` (chat B, ранний экспорт без user-строк)
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-12-15.txt` (файл `…-12-15.txt`: разговор B с хвостом разговора A — артефакт O-31)

### Прогон 12:15 — O-31: сетевая база дописывала ходы чужих разговоров

**Цитаты (консоль, гейт `aiCmDebug`; файлы на `C:\Users\oleg\Desktop`):**
- **10:37** — экспорт chat B без user-строк: `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-10-37.txt` (ранний артефакт, объяснён O-30).
- **10:49** — fire №1 `10:49:05.823` (`convId=3yyqar…`, `textLen=13270`, `baseComplete=1`, `histSource=memory`), `skip reason=already-fired` в `10:49:23.711`; файлы `…-10-49.txt` и `…-10-49 (1).txt` (O-11 round 1).
- **12:14:16–12:15:36** — `svc-emit-trace`: `convId=kSqqasn…` с `msgs=10` по базе (8 своих ходов + 2 чужих), `netMsgs` 8→10→18→20 при `domMsgs` 8↔2; fire `12:15:25.281`; файл `…-12-15.txt` с хвостом разговора A.

**Инспекция (read-only, до фикса):** накопление — `core/google-search-intercept.js:264-295` (`mergeTurns`: `threadId = currentThreadId || emittedThreadId`, threadId ответа не сверялся; ветка «меньше ходов» → union через `mergeStreamTurns` `:244-261`) и `core/content.js:391-394` (`lastBaseTexts = detail.messageTexts` без сверки с открытым разговором). **Сброс при смене треда — частичный:** `core/google-search-intercept.js:828-854` (`checkThreadSwitch`: сброс только в ветке «кэша нет» `:843-852`, а при наличии кэша база заменяется и поздние ответы старого треда всё равно дописываются); `core/content.js:347-353` (сброс по threadId снимка). Строка вывода инспекции: `накопление: core/google-search-intercept.js:264-295 + core/content.js:391-394, сброс при смене треда: частичный (есть только в ветке «кэша нет» google-search-intercept.js:843-852)`.

**Статусы после фикса `deafd59`:**
- **O-31 закрыт:** база и счётчики сегментированы по разговору — `isForeignThread` / `absorbForeignSnapshot` / `activateThread` в перехватчике (MAIN) + `aiCmSnapshotIsCurrentThread` / `aiCmDomThreadId` в content.js (ISOLATED); чужой снимок не эмитится, pct и экспорт считаются только по ходам текущего разговора; `histWritePath`-семантика и байты одиночного разговора прежние (пин `buildDetail`).
- **O-30 закрыт (объяснён путём записи базы):** в файл шла склейка двух разговоров, а не «DOM vs memory»; после O-31 состав канонический per-conversation.
- **O-27 (Medium-High) → ЗАКРЫТ (`d11f3d6`), затем защитный фикс (`4ee6eee`):** captcha/«подозрительный трафик» после пагинационного дозапроса folwr; на captcha-странице автоэкспорт скачал мусор `)]}' [""]`. Фикс `d11f3d6`: гард сетевого писателя (≥1 непустой ход; сырое XSSI-тело ходом не считается) + гейт автоэкспорта по признаку чат-страницы/непустой базе + сброс состояния прошлого документа на не-чат документе. **Защитный фикс `4ee6eee`:** гард сужен до единственной известной формы мусора (`isGarbageBody` = XSSI-префикс + 0 непустых ходов), путь пустого имени закрыт пост-гардом единственной точки скачивания (`download-blocked reason=xssi-prefix|empty-name`). Юнит-приёмка — пины O-27 + 16 пинов R-D (`tests/gsa-o27-protective-form-guard.test.js`), сьют 88/1495/6; живая captcha не воспроизводима по требованию (граница зафиксирована, см. раздел «O-27 — закрытие» ниже).
- **O-28 закрыт по дизайну:** молчание автоэкспорта до дозревания базы — гейт `baseComplete`.
- **O-29 ЗАКРЫТ живой приёмкой 2026-09-22:** антиспам-гард `aiCmAutoExportLastLogState` (family→сигнатура) + хелперы `aiCmAutoExportLogOnChange`/`aiCmAutoExportLogFiredBit`; лог только на смену состояния; сьют 117/2096/6/0 (+24 пина). Наблюдение: race condition memory-snapshot vs network-finalize (дельта 262 мс) — вне scope.
- **O-11:** живая приёмка (два файра в одну минуту → `-2`) была заблокирована до O-31 (состав базы недостоверен) и проходится тем же прогоном после перезагрузки расширения с `deafd59`; **пройдена в прогоне 15:58 — см. ниже (ЗАКРЫТА).**

### Прогон 15:58 — живая приёмка O-11 + O-31 (релиз v2.0.5)

**Параметры:** тот же живой GSA-прогон после перезагрузки расширения с кодом O-11 (раунд 2) + O-31; порог 1%, формат txt, два разговора (A — короткие ходы, B — вставленный blob), SPA-переключение; консоль под гейтом `aiCmDebug`.

**Цитаты (файлы на `C:\Users\oleg\Desktop`):**
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-15-58.txt` — msgs=8, textLen=5121, pct=1.4
- `ai-context-monitor-google_search-Gemini-Search-AI-2026-09-16-15-58-2.txt` — msgs=2, textLen=13270, pct=3.5

**Вердикт (ЗАКРЫТО):**
- **O-11 ЗАКРЫТ живой приёмкой:** два файра в одну минуту дали два РАЗНЫХ имени, второе — с нашим суффиксом `-2` (`…-15-58.txt` и `…-15-58-2.txt`); chrome-овского суффикса ` (1)` нет. Маска имён из `buildAutoExportFileName`, реестр синхронно резервирует имя до старта скачивания.
- **O-31 ЗАКРЫТ живой приёмкой:** базы чистые per-conversation — каждый файл содержит только ходы своего разговора (чужих хвостов нет), msgs=8/2 (не 10 и не 18), pct стабилен 1.4%/3.5% и считается по ходам текущего разговора.
- **O-30** подтверждён: после сегментации состав канонический per-conversation (прежняя склейка двух разговоров не воспроизводится).

### Прогон 18:47 — измерение O-32 (воспроизведения нет) + основание защитного фикса O-27

**Параметры:** GSA, гейт `aiCmDebug`, порог 1%, формат txt, разговор `kSqqasnVMIrOhbIP3cjn6QY`; файл лога — `Логи..txt` на `C:\Users\oleg\Desktop`.

**Цитаты (строки лога):**
- `18:47:16.080 gsa-base-write path=open verdict=submit … shape=turns=5 empty=0 userLen=403 asstLen=5951 maxLen=2644 … parsed=5 folwrContainers=5 domContainers=5 bodyLen=1355398`
- `18:47:16.080 gsa-base-write path=applyTurns verdict=accept … baseComplete=0 validation=hasUsableTurns=true`
- `18:47:16.563 gsa-base-write path=probe verdict=submit-final … complete=1 addedTotal=0 steps=1 reason=probe-cursor-repeat`
- `18:47:16.662 [auto-export] site=google_search skip reason=not-complete … pct=1.5`; `18:47:42.767 badge-update pct=1.5% tokens=1882`

**Вердикт:**
- **Основание защитного фикса O-27 (`4ee6eee`):** на легитимном теле GSA валидатор не отклоняет НИ ОДНОГО хода ни на одном пути (open / applyTurns / probe) — `empty=0`, `hasUsableTurns=true`. Значит, сужение гарда до точной формы мусора безопасно для здорового пути.
- **O-32 остаётся ОТКРЫТ:** механизм индикатора/процента не воспроизведён — 4 прогона под гейтом `aiCmDebug` воспроизведения не дали (в этом логе `pct=1.5`/`tokens=1882` стабильны, скачков бейджа нет). В коммите `4ee6eee` O-32 не трогается: нет основания без воспроизведения.

### O-27 — закрытие (`d11f3d6`) + защитный фикс по форме мусора (`4ee6eee`)

**Живой дефект (10:46):** Google ответил сервисной страницей captcha / «подозрительный трафик» (вероятная реакция на пагинационный дозапрос folwr, raw 1 195 742 B); на этой странице автоэкспорт сработал и скачал мусор `)]}' [""]` (файл `f.txt`).

**Инспекция (read-only, три строки вывода):**
- `captcha-путь базы: core/google-search-intercept.js:205-220 (isUsableTurn/hasUsableTurns — добавлены фиксом; до фикса валидации содержимого не было) + писатели :366-380 mergeTurns, :607-618 applyTurns (folwr-open, passive folwr/folif, XHR, probe/пагинация) → buildDetail :234-260 historyComplete по умолчанию true → core/content.js:429 baseComplete; валидация была только «turns.length > 0»`
- `pct после навигации: память экземпляра — core/state.js:26 lastPercentage (и maxTokenCount/autoExportLastPct :139), сброс −1 при новом чате core/widget.js:150,171; sessionStorage для pct не используется; сам pct в момент файра ПЕРЕСЧИТЫВАЕТСЯ в DRAW core/content.js:1509-1511 → maybeAutoExport(percentage) :1537; re-check-пути берут autoExportLastPct`
- `гейт на не-чат странице: до фикса — только enabled/percentage/порог/baseComplete|baseSeen/латч (utils/export-emit-pipeline.js: shouldSkipAutoExport, теперь :340-360); после фикса + shouldSkipGsaPageGuard :322-336 (msgs≥1, непустой текст базы, признак чат-страницы: threadId снаффнут ИЛИ чат-контейнер в DOM)`

**Фикс:**
- **(а) писатель базы:** `applyTurns`/`mergeTurns` требуют ≥1 НЕПУСТОЙ ход; ход, доехавший сырым телом Google (XSSI-префикс из 4 символов), ходом не считается. Тело без ходов не пишет базу, не эмитит снимок и не взводит `baseComplete`. Нормальное folwr-тело пишется байтово прежним путём (пин сравнения «до/после» на фикстуре `tests/fixtures/google-folwr-open.html`).
- **(б) гейт автоэкспорта:** `shouldSkipGsaPageGuard` — msgs≥1, непустой текст базы, признак чат-страницы; причины `not-chat-page` / `no-messages` / `empty-base`. Латч `fired` на таких skip НЕ ставится (поздний честный экспорт на настоящей чат-странице остаётся возможен). Поля необязательны: прочие сайты и прежние вызовы — 1:1. Пороги на чат-страницах не тронуты.
- **(в) состояние прошлого документа:** `aiCmGsaChatPageMarker` (content.js:338) + `aiCmGsaResetStaleStateOnChatlessDoc` (content.js:353, вызов на входе DRAW :1244) — база/pct/pct-latch прошлого документа гасятся на не-чат документе и всё равно не проходят гейт. Чат-страница не трогается: байты и маска экспорта чат-страниц прежние.
- **НЕ тронуто:** байты/маска экспорта чат-страниц, пороговые гейты, латч и реестр имён (O-11), `CHANGELOG.md`, `privacy.html`, `release.yml`.

**Юнит-приёмка:** `tests/gsa-o27-captcha-base-guard.test.js` — 23 пина: captcha-подобное тело (XSSI-префикс + пустой payload) эмитится → база не пишется, файра нет; контейнер без содержимого не пишет базу и не ставит ПОЛНАЯ; нормальное folwr-тело → база байтово прежняя; навигационная протечка (состояние прошлого документа на не-чат документе) → гейт не проходит; реальный `maybeAutoExport` с признаком чат-страницы; регресс O-11/O-31. Сьют: **86 suites, 1462 passed, 6 skipped, 0 failed**; `node --check` по всем изменённым файлам.

**Граница:** живая captcha не воспроизводима по требованию (Google отдаёт её по своей bot-detection) — живой приёмкой закрыт только юнит-контур; воспроизведение возможно при следующем естественном срабатывании (проверить: файла нет, в логе `skip reason=not-chat-page`, в базе нет тела с XSSI-префиксом).

### O-27 — защитный фикс по единственной известной форме мусора (`4ee6eee`)

**Основание:** живой артефакт `f.txt` = `)]}'` + перевод строки + `[""]` + перевод строки (XSSI-префикс Google с пустым payload), скачан на captcha-странице с ПУСТЫМ именем файла. Логи прогона 18:47 (`Логи..txt`) доказывают: на легитимном теле GSA валидатор НИЧЕГО не отклоняет во всех путях — `gsa-base-write path=open|applyTurns|probe … shape=turns=5 empty=0 … validation=hasUsableTurns=true`. Значит, сужение гарда до точной формы мусора безопасно для здорового пути.

**Фикс:**
- **(а) гард базы сужен до точной формы:** `isUsableTurn` больше не отбрасывает ход по тексту, начинающемуся XSSI-префиксом (ход пригоден, если текст непуст); форма мусора решается ТЕЛОМ вместе с разбором — `isGarbageBody(bodyText, turns)` = тело начинается `)]}'` И разбор дал 0 непустых ходов. `applyTurns`/`mergeTurns` принимают сырое тело ответа, и на всех сетевых путях (`open`, пагинация, `probe` step/finish, passive `folwr`/`folif`, XHR) режется только эта форма: база не пишется, снимок не эмитится, `baseComplete` не взводится (вердикт `reason=xssi-prefix`). Легитимное тело — в т.ч. начинающееся тем же префиксом, но с ходами, — пишется байтово прежним путём; DOM-добор (`scroll-backfill`) тела не имеет.
- **(б) пост-гард единственной точки скачивания:** `utils/export-text-builders.js:downloadBlob` (через неё идёт `aiCmAutoExportStartDownload`) не выдаёт файл и печатает `[AI CM][diag] download-blocked reason=xssi-prefix|empty-name`, если первые байты контента начинаются `)]}'` ИЛИ имя файла пусто/не задано. Путь пустого имени закрыт независимо от триггера (автоэкспорт / options / печать). Канонический хелпер — `utils/debug.js:aiCmDiagDownloadBlocked`; строка только под гейтом `aiCmDebug`; байты легитимного экспорта не меняются.
- **НЕ тронуто:** механизм индикатора/процента (O-32 — остаётся открыт), байты и маска экспорта легитимной базы, латч и реестр имён O-11, сегментация O-31, `CHANGELOG.md`, `privacy.html`, `release.yml`. Версия/CHANGELOG по scope-lock задачи не менялись (пункт 5 DHS_RULES приостановлен явным allow-листом промпта).

**Юнит-приёмка (правило R-D):** `tests/gsa-o27-protective-form-guard.test.js` — 16 пинов: легитимное тело (прод-фикстура и форма `turns=5 empty=0` из логов) → снимок и txt-экспорт байтово прежние, download не блокируется; XSSI-тело → база не пишется, `baseComplete` не взводится, download blocked `reason=xssi-prefix` (и через `aiCmAutoExportStartDownload`); пустое имя → blocked `reason=empty-name`; регресс O-11 (два файра за минуту → `-2`), O-31, O-15…O-20 (BOM-экспорт); диаг-строки только под гейтом, байты blob при гейте вкл/выкл идентичны. Обновлены пины `tests/gsa-o27-captcha-base-guard.test.js`, `tests/diag-o27-o32-instrumentation.test.js`, песочница O-31 и F1-пин probe-вызова. Сьют: **88 suites, 1495 passed, 6 skipped, 0 failed**; `node --check` по всем изменённым файлам.

**Граница:** закрыта единственная ИЗВЕСТНАЯ форма мусора (`f.txt`) и путь пустого имени; иных форм из живых прогонов нет, поэтому гард не расширялся (правило «два провала → измерение»: механизм не воспроизведён — не трогаем).

**Платформенная заметка:** пагинационные folwr-дозапросы (raw ~1.2 МБ, до 12 страниц за открытие + probe) могут триггерить bot-detection Google (captcha / «подозрительный трафик», после чего async-ответы приходят с пустым payload). В живых прогонах учитывать: не гнать длинную историю одним заходом, давать паузу между прогонами, при captcha прекращать прогон (circuit-breaker `429/sorry` глушит свои запросы на 10 мин) и проверять файлы автоэкспорта на мусор.

## Переоткрытие O-32 (2026-09-23, приоритет Medium) → ЗАКРЫТ живой приёмкой 2026-09-23

**Симптом (переоткрытие):** Инфляция токенов после SPA-возврата в GSA. Бейдж вырос с `4.4%` (5658 токенов, msgs=6, textLen=12255) до `9.0%` (11475 токенов, msgs=18, textLen=24764) без отправки новых сообщений пользователем.

**Механизм (переоткрытие):** При SPA-возврате сработал путь `gsa-base-write path=passive-folif`, который подтянул полную сетевую историю треда (`netMsgs=18`). DOM на момент возврата содержал только 12 сообщений (`domMsgs=12`). Расширение использовало сетевой счётчик для расчёта токенов, что привело к удвоению оценки без реального роста контекста.

**Диагностика (переоткрытие):** Логи зафиксированы в `Логи..txt` (метки времени 16:45:05 → 16:47:48). Строки `gsa-state`, `gsa-base-write`, `badge-update` согласованы и подтверждают рассогласование.

**Фикс (2026-09-23):** Инвариант канонической идентичности ходов GSA реализован DSH (14 файлов, +485/−47): `canonicalTurnKeyOf` / `canonicalAnswerBody` / `canonicalizeTurnForThread` / `mergeTurnsMonotone` в `utils/google-search-folwr-parser.js`; `historyComplete === true` (дефолт полноты убран) + канонический ключ + `seedSeenKeys` во ВСЕХ 5 точках в `core/google-search-intercept.js`; неполная база = канонический пол в `core/hybrid-tail.js`; `useMonotonic` при неполной базе + маркер `gsa-metric-src` в `core/content.js`; дедуп экспортного источника для `google_search` в `core/base-handler.js`. Сьют 120/2167/6/0 (+23 пина `tests/o32-gsa-canonical-thread-invariant.test.js`).

**Вердикт (2026-09-23 19:13):** ЗАКРЫТ живой приёмкой. Инфляция устранена: бейдж 4.6% (5867 токенов, msgs=12) при открытии → 4.5% (5818) при F5 → 4.6% (5867) при SPA-возврате → 7.8% (10016) после +2 обменов → 7.8% (10040) при F5 → 7.9% (10071) при SPA-возврате. Хеш отпечатка стабилен: 0f9f7baa для 6 ходов, 32e4a45b для 8 ходов на всех точках навигации.

## Следующие шаги

1. ✅ Релизы v2.0.0 → v2.0.1 → v2.0.2 (тег `v2.0.2`, GitHub Release)
2. ✅ Декомпозиция: этапы 1/3, 2/3, 3/3 — все приняты
3. ✅ CI-хотфикс (`322fd7e`) — Lint на main зелёный
4. ✅ O-15…O-18 DeepSeek-экспорт — закрыты живыми прогонами
5. ✅ O-20 санация инъекций — реализована и в main (`f71f7d1`)
6. ✅ O-19 приёмочный прогон с соседями — закрыт (инъекции соседа остаются в серверной истории); O-25 отклонена; O-26 диагностика — в main (`1e0bb14`)
7. ✅ O-9-подпись: подпись `snapshot-at-manual` берёт реальные ходы и тексты из канонической базы — в main (`59a295f`); live-приёмка Perplexity `msgs=4` при эталоне 4
8. ✅ Выпустить v2.0.3 — релиз опубликован (тег `v2.0.3`, GitHub Release)
9. ✅ Выпустить v2.0.4 — релиз опубликован (тег `v2.0.4`, коммит `a7f73a8`, GitHub Release)
10. ✅ O-11: единый источник имени автоэкспорта + гард коллизии на GSA — в main (`3605375`); сьют-пин: два автоэкспорта в одну минуту → два разных имени, обе копии целы
11. ✅ O-11 раунд 2: единственная точка старта скачивания автоэкспорта (`aiCmAutoExportStartDownload`) с синхронным реестром имён ДО старта — в main (`07c83a5`); инспекция: обход реестра GSA-путём опровергнут; живая приёмка требует перезагрузки расширения и повторного прогона
12. ✅ O-31: сегментация сетевой базы GSA по разговору (threadId) — в main (`deafd59`, не запушен); O-30 закрыт объяснением пути записи базы; тесты 85/1439/6; живая приёмка O-11 разблокирована (тот же прогон после перезагрузки расширения)
13. ✅ Живая приёмка 15:58 (O-11 + O-31) — ЗАКРЫТА: файлы `…-15-58.txt` / `…-15-58-2.txt` (msgs=8/2, textLen=5121/13270, pct=1.4/3.5), базы чистые per-conversation; выпущен релизный коммит v2.0.5 (`a46bbac` — манифест/пакет 2.0.5, CHANGELOG, версионные пины; локально, НЕ запушен)
14. ✅ O-27: гард базы (≥1 непустой ход в сетевом писателе) + гейт автоэкспорта по признаку чат-страницы/непустой базе + сброс состояния прошлого документа на не-чат документе GSA — в main (`d11f3d6`, не запушен); юнит-приёмка 23 пина (`tests/gsa-o27-captcha-base-guard.test.js`), сьют 86/1462/6; живая captcha не воспроизводима по требованию — граница зафиксирована
15. ✅ O-27 защитный фикс (`4ee6eee`, не запушен): гард базы СУЖЕН до единственной известной формы мусора (`)]}'` + 0 непустых ходов; пост-гард единственной точки скачивания (`downloadBlob`) — `download-blocked reason=xssi-prefix|empty-name` (закрыт путь пустого имени); пины R-D `tests/gsa-o27-protective-form-guard.test.js` (16), сьют **88/1495/6**; live-приёмка 19:48 — здоровый чат экспортируется байтово как прежде, **O-27 ЗАКРЫТ**; O-32 переведён в низкий приоритет (механизм не воспроизведён)
16. ✅ Выпустить v2.0.6 — релизный коммит `ce3b257` (манифест/пакет 2.0.6, CHANGELOG, версионные пины; локально, НЕ запушен), тег `v2.0.6` переставлен на него; сьют **88/1495/6**
17. ✅ O-1 (`7ea8273`, не запушен): щит бейджа ChatGPT — взвод на старте документа независимо от convId, отпускание по `baseSeen`/фолбэк 10 c, плейсхолдер P2a, 16 R-D пинов (`tests/chatgpt-o1-badge-hold.test.js`); живая приёмка 10:21–10:22 — **O-1 ЗАКРЫТ**; сьют **89/1511/6**; границы: форма armed-empty-convid живьём не воспроизвелась, серия оборвана лимитом платформы
18. ✅ Выпустить v2.0.7 — релизный коммит `52bd2cc` (манифест/пакет 2.0.7, CHANGELOG `[2.0.7]`, версионные пины); **опубликован** — тег `v2.0.7` на `ae35785` (= `origin/main`), GitHub Release
19. ✅ O-33 (`c1fecb8`; релизный коммит `b42a269`): блок автоэкспорта по DOM-базе до сетевого снимка (`reason base-pending` после порогов), лог только на смене состояния, 24 R-D пина; живая приёмка 2026-09-17 15:33–15:42 (2×F5 + realtime — файра по DOM-базе нет, файр только после `baseSeen=1`, двойных файров нет — `already-fired`) — **O-33 ЗАКРЫТ**; сьют **90/1535/6**; граница: `reason base-pending` вживую не воспроизведён (DOM pct < порога 1%) — прецедент O-27
20. ✅ O-32 (голова, Medium) ЗАКРЫТ живой приёмкой 2026-09-23: инвариант канонической идентичности ходов GSA; сьют 120/2167/6/0 (+23 пина); бейдж стабилен при F5/SPA/+2 обменах
21. ⏳ **Очередь:** O-21 (голова, Low); O-46 ЗАКРЫТ 2026-09-23 (коммит d3d0ea0)
✅ **Выпустить v2.0.8** — релизный коммит `b42a269` (`manifest.json`/`package.json` 2.0.8, CHANGELOG `[2.0.8]`, версионные пины); тег `v2.0.8` на `1d3de6c`; **опубликован** (GitHub Release)
22. ✅ O-7 (`76c2a31` + `75d1db1`): OFF-зачистка S1–S5 + релизный бамп v2.0.9; живая приёмка 2026-09-18 09:37–09:54 — **ЗАКРЫТА**; сьют **91/1596/6**; тег `v2.0.9` на `e07cfa4`, push выполнен
23. ⏳ **GitHub Release v2.0.9** — шаг владельца (создать релиз на теге v2.0.9)
